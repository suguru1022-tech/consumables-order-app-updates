# 消耗品管理・発注アプリ 引き継ぎ情報

この文書は、Google Apps Script 製「消耗品管理・発注アプリ」の正式な引き継ぎ情報です。今後の開発では、作業前に必ず本書を参照してください。

本書と既存コードの挙動に矛盾がある場合は、どちらかを推測で変更せず、差異と影響を整理してユーザーへ確認してください。

## 1. アプリの目的

複数店舗で使用する消耗品の在庫確認、発注依頼、本部発注処理を一つにまとめた社内 Web アプリです。

店舗スタッフはスマホまたは PC で在庫を確認し、不足している消耗品を本部へ発注依頼します。本部は依頼内容を確認し、登録済みの商品ページから発注して、「手配済み」「納品済み」まで管理します。発注依頼時には、指定された本部メールアドレスへメールを送信します。

## 2. 技術構成と正式ソース

Google Apps Script と Google スプレッドシートで構築しています。

唯一の正式ソースは次の3ファイルです。

- `src/Code.gs`
- `src/Index.html`
- `src/appsscript.json`

今後の開発では `src/` だけを編集元として扱い、release package を編集元にしません。

Google スプレッドシートでは、主に次のシートを使用します。

- `Products`
- `Settings`
- `Inventory`
- `Orders`

## 3. 店舗側「在庫・発注」画面

- 店舗を選択し、担当者名を入力して使用します。
- 商品はカテゴリ別に表示し、カテゴリは折りたたみ式とします。
- 商品ごとに「現在庫」と「発注数」を横並びで表示します。
- 現在庫側は淡いグレー系、発注側は淡いベージュ系を基本とし、役割を視覚的に区別します。
- スマホ操作を最優先します。
- 数字入力は `− / 数字 / ＋` のステッパーと、数字タップ時のスクロール選択を併用します。
- 現在庫の変更は自動保存し、「保存中…」「保存済み」などのフィードバックを表示します。
- 最低在庫以下の商品は「要確認」、問題がない商品は「在庫OK」と表示します。
- 「画像を見る」ボタンは商品画像が登録されている場合だけ表示します。
- 商品画像は常時表示せず、必要なときだけポップアップで確認できる仕様を維持します。

## 4. 在庫単位と発注単位

在庫単位と購入単位は別に管理します。

レジロールの例：

- 在庫単位：巻
- 1発注あたり数量：10巻、20巻など商品設定による
- 発注単位名：セット
- 発注数：1

店舗画面、本部画面、発注メールでは、`1セット（合計10巻）` のように意味が分かる表示にします。

商品ごとに次の項目を設定可能にします。

- 在庫単位
- 1発注あたり数量
- 発注単位名
- 推奨発注数

## 5. 発注リスト

店舗スタッフは必要な商品を発注リストへ追加し、まとめて本部へ送信します。

本部メールには次の情報を含めます。

- 店舗名
- 担当者
- 発注日時
- 商品名
- 発注数
- 購入単位
- 実数量
- 備考

発注履歴は `Orders` シートにも保存します。

## 6. 本部発注画面

本部画面は PIN でロックします。縦に長くなりすぎないよう、内部を次のタブに分けます。

- `発注処理`
- `仕入先・画像`
- `商品マスター`
- `システム更新`

本部画面を開いた直後は、日常的に使用する「発注処理」を中心に表示します。

## 7. 発注処理

店舗から届いた発注依頼を一覧表示し、商品名、数量、店舗、担当者、発注ID、状態を確認できるようにします。

状態は基本的に次の3種類です。

- `発注依頼`
- `手配済み`
- `納品済み`

商品ごとに登録済みの仕入先ページへ「発注ページを開く」から移動できるようにします。二重発注を防ぐため、状態と状態変更の結果を明確に表示します。

## 8. 仕入先・商品画像

商品ごとに次の情報を登録できます。

- 仕入先名
- 発注ページURL
- 商品画像
- 商品メモ

商品画像は本部画面から直接アップロードし、Google Drive へ保存します。発注時に商品を識別するための機能ですが、通常画面を画像で埋めず、画像やメモは必要なときだけ確認できる設計を維持します。

## 9. 商品マスター管理

- `Products` シートを直接編集しなくても、本部画面から商品を追加・編集できる仕様とします。
- 新規商品の商品IDは、`P030`、`P031`、`P032` のように自動採番します。
- 商品の完全削除より「有効 / 無効」での管理を優先し、過去の在庫履歴や発注履歴を保護します。
- 新しいカテゴリを登録した場合は、店舗画面へ自動的にカテゴリとして表示します。

## 10. UI/UX 方針

- 店舗スタッフが説明なしでも使えることを最優先します。
- 特にスマホ表示を重視し、ボタンは十分な大きさを確保します。
- PC ではマウスオーバー時に背景色、文字色、枠色などを変え、押せる場所を明確にします。
- スマホではタップ時に軽い押下感を出します。
- 保存やアップロードなど時間がかかる操作では、押した直後にボタンを「保存中…」「アップロード中…」などへ変更し、処理中の二重押下を防止します。
- 完了・エラー通知は、現在画面の上部にトースト形式で表示し、ページ最上部へ戻らなくても確認できるようにします。

## 11. データ保護

既存のスプレッドシートデータを絶対に壊さないでください。特に次のシートの既存データを維持します。

- `Inventory`
- `Orders`
- `Products`
- `Settings`

`setup()` は新規導入専用です。既に運用中の環境では、明示的な指示なしに絶対に実行しません。

シート構造を変更する場合は、既存データを保持する安全な upgrade / migration 処理を必ず作成します。

## 12. 自動更新システム

アプリには GitHub 経由の自動更新機能があります。

- GitHub リポジトリ：`suguru1022-tech/consumables-order-app-updates`
- 更新マニフェスト：`release-manifest.json`
- バージョン別更新パッケージ：`release-package-vX.X.X.json`

過去の release package は削除しません。`release-manifest.json` だけを、常に最新版のパッケージを指すように更新します。

自動更新前には、現在のコードを Google Drive へバックアップします。

## 13. Web アプリデプロイ事故対策

過去に、自動更新時に Web アプリではなくライブラリデプロイを更新し、Web アプリURLが使用不能になった事故がありました。

この再発を防ぐため、`Settings` に次の値を固定保存しています。

- `WebアプリデプロイID`
- `WebアプリURL`

自動更新では、必ず固定された Web アプリデプロイIDだけを更新します。更新前に対象デプロイが `WEB_APP` であることを Apps Script API で検証し、`WEB_APP` でない場合は更新を中止します。

ライブラリデプロイを自動更新対象にしてはいけません。`src/appsscript.json` の Web アプリ設定や OAuth スコープを、明示的な確認なしに削除・変更してはいけません。

## 14. Google 側で設定済みの項目

次の設定は完了済みであることを前提とします。

- Google Cloud の標準 GCP プロジェクトへ移行済み
- Apps Script API を有効化済み
- Google Drive API を有効化済み
- Google Auth Platform / OAuth を設定済み
- ユーザー側の Google Apps Script API 利用設定を有効化済み

## 15. 今後のリリース手順

機能変更の依頼を受けた場合は、原則として次の手順を一回の作業で完了します。

1. `src/` の正式ソースを修正する。
2. バージョン番号を上げる。
3. 必要であれば、安全な migration 関数を追加する。
4. `release-package-vX.X.X.json` を生成する。
5. `release-manifest.json` を最新版へ更新する。
6. 過去の release package を保持する。
7. 内容と整合性を確認する。
8. `main` へ commit する。
9. GitHub へ push する。
10. 新バージョン番号、変更内容、コミットIDを報告する。

ユーザー側では原則として、`本部発注 → システム更新 → 最新版を確認 → 最新版に更新` だけで反映できる状態を維持します。

## 16. 開発時の重要方針

- 「動けばいい」ではなく、実店舗のスタッフが毎日使う前提で開発します。
- 操作数の削減、発注漏れの防止、二重発注の防止、スマホでの見やすさを優先します。
- 既存機能を変更するときは、これまでの運用を壊さないことを優先します。
- GitHub の `src/` と release package の内容を一致させます。
- 更新後に `APP_VERSION` と `release-manifest.json` の `version` が一致することを必ず確認します。
- 本書と既存コードに矛盾が見つかった場合は、勝手に変更せずユーザーへ確認します。
