{
  "version": "6.6.7",
  "files": [
    {
      "name": "Code",
      "type": "SERVER_JS",
      "source": "const SHEETS = {\n  PRODUCTS: 'Products',\n  INVENTORY: 'Inventory',\n  ORDERS: 'Orders',\n  SETTINGS: 'Settings'\n};\n\nconst APP_VERSION = '6.6.7';\nconst UPDATER_MARKER = '__FUNFIELDS_SELF_UPDATER_V1__';\nconst DEFAULT_UPDATE_MANIFEST_URL = 'https://raw.githubusercontent.com/suguru1022-tech/consumables-order-app-updates/refs/heads/main/release-manifest.json';\nconst UPDATE_MANIFEST_URL_PROPERTY = 'UPDATE_MANIFEST_URL';\nconst V6_4_0_PRODUCT_MIGRATION_KEY = '商品マスター移行_v6.4.0';\nconst APP_CACHE_KEYS = {\n  PRODUCTS: 'consumables_products_v1',\n  INVENTORY_PREFIX: 'consumables_inventory_v1_'\n};\nconst V6_4_0_PRODUCTS = [\n  ['梱包用品','OPP袋（4枚セット用）'],\n  ['オリパ用品','ブロックオリパ用袋（100枚セット用）'],\n  ['オリパ用品','オリパ用紙袋'],\n  ['カード用品','ストレージ用ケース'],\n  ['ガチャ用品','ガチャ用白箱'],\n  ['会員用品','メンバーズカード'],\n  ['店舗備品','マイドテープ'],\n  ['事務用品','テプラテープ'],\n  ['事務用品','セロハンテープ'],\n  ['事務用品','A4ラミネートフィルム'],\n  ['事務用品','A3ラミネートフィルム'],\n  ['清掃用品','ゴミ袋'],\n  ['ガチャ用品','カプセルガチャ用平型ヒューズ'],\n  ['プリンター','プリンター用インク（稲沢）ブラック'],\n  ['プリンター','プリンター用インク（稲沢）マゼンタ'],\n  ['プリンター','プリンター用インク（稲沢）シアン'],\n  ['プリンター','プリンター用インク（稲沢）イエロー'],\n  ['プリンター','プリンター用インク（大須）ブラック'],\n  ['プリンター','プリンター用インク（大須）マゼンタ'],\n  ['プリンター','プリンター用インク（大須）シアン'],\n  ['プリンター','プリンター用インク（大須）イエロー'],\n  ['レンタルショーケース','レンタルショーケース管理番号シール'],\n  ['レンタルショーケース','レンタルショーケース価格シール'],\n  ['事務用品','コピー用紙 A3']\n];\n\nfunction doGet() {\n  const template = HtmlService.createTemplateFromFile('Index');\n  template.appVersion = APP_VERSION;\n  return template\n    .evaluate()\n    .setTitle('消耗品管理・発注')\n    .setXFrameOptionsMode(HtmlService.XFrameOptionsMode.ALLOWALL);\n}\n\n// 新規導入専用。既に運用中の場合は再実行しないでください。\nfunction setup() {\n  const ss = SpreadsheetApp.getActiveSpreadsheet();\n  const productRows = [\n    ['P001','レジ・決済','レジロール','巻',5,1,1,true,'','','','',10,'セット'],\n    ['P002','レジ・決済','PayCas専用レジロール','巻',3,5,1,true,'','','','',1,'巻'],\n    ['P003','袋','無料ショップ袋','束',3,5,1,true,'','','','',1,'束'],\n    ['P004','袋','有料ショップ袋 Sサイズ','束',2,5,1,true,'','','','',1,'束'],\n    ['P005','袋','有料ショップ袋 Mサイズ','束',2,5,1,true,'','','','',1,'束'],\n    ['P006','袋','有料ショップ袋 Lサイズ','束',2,5,1,true,'','','','',1,'束'],\n    ['P007','事務用品','コピー用紙 A4','箱',1,2,1,true,'','','','',1,'箱'],\n    ['P008','カード用品','業務用スリーブ','箱',2,5,1,true,'','','','',1,'箱'],\n    ['P009','カード用品','業務用ローダー','箱',2,5,1,true,'','','','',1,'箱'],\n    ['P010','ラベル','POS専用ラベル','巻',2,5,1,true,'','','','',1,'巻'],\n    ['P011','ラベル','ハンドラベラー用ラベル','巻',2,5,1,true,'','','','',1,'巻'],\n    ['P012','清掃用品','モップ','本',1,1,1,true,'','','','',1,'本'],\n    ['P013','清掃用品','窓用アルコール','本',1,2,1,true,'','','','',1,'本'],\n    ['P014','清掃用品','雑巾','枚',5,10,1,true,'','','','',1,'枚'],\n    ['P015','ラベル','マイラベル','巻',2,5,1,true,'','','','',1,'巻'],\n    ['P016','プリンター','プリンター用インク','個',1,2,1,true,'','','','',1,'個'],\n    ['P017','オリパ用品','オリパ用スリーブ 赤','箱',1,3,1,true,'','','','',1,'箱'],\n    ['P018','オリパ用品','オリパ用スリーブ 黒','箱',1,3,1,true,'','','','',1,'箱'],\n    ['P019','オリパ用品','オリパ用スリーブ 白','箱',1,3,1,true,'','','','',1,'箱'],\n    ['P020','オリパ用品','オリパ用スリーブ 黄色','箱',1,3,1,true,'','','','',1,'箱'],\n    ['P021','オリパ用品','オリパ用スリーブ 青','箱',1,3,1,true,'','','','',1,'箱'],\n    ['P022','オリパ用品','オリパ用スリーブ 緑','箱',1,3,1,true,'','','','',1,'箱'],\n    ['P023','オリパ用品','オリパ用袋 赤','束',1,3,1,true,'','','','',1,'束'],\n    ['P024','オリパ用品','オリパ用袋 青','束',1,3,1,true,'','','','',1,'束'],\n    ['P025','オリパ用品','オリパ用袋 黒','束',1,3,1,true,'','','','',1,'束'],\n    ['P026','オリパ用品','オリパ用袋 緑','束',1,3,1,true,'','','','',1,'束'],\n    ['P027','オリパ用品','オリパ用袋 オレンジ','束',1,3,1,true,'','','','',1,'束'],\n    ['P028','PSA用品','PSA用オリパ袋','束',1,3,1,true,'','','','',1,'束'],\n    ['P029','PSA用品','PSA保護袋','束',1,3,1,true,'','','','',1,'束'],\n    ...V6_4_0_PRODUCTS.map(function(p, index) {\n      return ['P' + Utilities.formatString('%03d', 30 + index),p[0],p[1],'個',1,1,1,true,'','','','',1,'個'];\n    })\n  ];\n\n  const products = getOrCreateSheet_(ss, SHEETS.PRODUCTS);\n  products.clear();\n  products.getRange(1,1,1,16).setValues([['商品ID','カテゴリ','商品名','在庫単位','最低在庫','推奨発注数（購入単位）','発注数ステップ','有効','仕入先','発注ページURL','商品画像URL','商品メモ','1発注あたり数量','発注単位名','対象外店舗','表示順']]);\n  products.getRange(2,1,productRows.length,14).setValues(productRows);\n  products.getRange(2,16,productRows.length,1).setValues(productRows.map(function(_, index){return [index + 1];}));\n  products.setFrozenRows(1);\n\n  const settings = getOrCreateSheet_(ss, SHEETS.SETTINGS);\n  settings.clear();\n  settings.getRange(1,1,1,2).setValues([['設定項目','値']]);\n  settings.getRange(2,1,5,2).setValues([\n    ['本部発注メール','head-office@example.com'],\n    ['店舗一覧','稲沢本店,トレカダンジョンLite大須店'],\n    ['メール件名接頭辞','【消耗品発注依頼】'],\n    ['管理アプリ名','消耗品管理・発注'],\n    ['本部管理PIN','1234']\n  ]);\n  settings.setFrozenRows(1);\n\n  const inventory = getOrCreateSheet_(ss, SHEETS.INVENTORY);\n  inventory.clear();\n  inventory.getRange(1,1,1,6).setValues([['更新日時','店舗','商品ID','商品名','現在庫','更新者']]);\n  inventory.setFrozenRows(1);\n\n  const orders = getOrCreateSheet_(ss, SHEETS.ORDERS);\n  orders.clear();\n  orders.getRange(1,1,1,11).setValues([['発注ID','発注日時','店舗','発注者','商品ID','商品名','発注数','単位','状態','メール送信先','備考']]);\n  orders.setFrozenRows(1);\n\n  [products, settings, inventory, orders].forEach(s => s.autoResizeColumns(1, s.getLastColumn()));\n  return '初期設定が完了しました。';\n}\n\n// v2からv3へ更新する時に「1回だけ」実行してください。既存データは消しません。\nfunction upgradeToV3() {\n  const ss = SpreadsheetApp.getActiveSpreadsheet();\n  const products = ss.getSheetByName(SHEETS.PRODUCTS);\n  if (!products) throw new Error('Productsシートが見つかりません。');\n  if (products.getLastColumn() < 9) products.getRange(1,9).setValue('仕入先');\n  if (products.getLastColumn() < 10) products.getRange(1,10).setValue('発注ページURL');\n  if (!products.getRange(1,9).getValue()) products.getRange(1,9).setValue('仕入先');\n  if (!products.getRange(1,10).getValue()) products.getRange(1,10).setValue('発注ページURL');\n\n  const settings = ss.getSheetByName(SHEETS.SETTINGS);\n  if (!settings) throw new Error('Settingsシートが見つかりません。');\n  const vals = settings.getLastRow() > 1 ? settings.getRange(2,1,settings.getLastRow()-1,2).getValues() : [];\n  const hasPin = vals.some(r => r[0] === '本部管理PIN');\n  if (!hasPin) settings.appendRow(['本部管理PIN','1234']);\n  products.autoResizeColumns(1, Math.max(10, products.getLastColumn()));\n  return 'v3への更新が完了しました。Settingsシートの「本部管理PIN」を必ず変更してください。';\n}\n\n// v3からv4へ更新する時に「1回だけ」実行してください。既存データは消しません。\nfunction upgradeToV4() {\n  const ss = SpreadsheetApp.getActiveSpreadsheet();\n  const products = ss.getSheetByName(SHEETS.PRODUCTS);\n  if (!products) throw new Error('Productsシートが見つかりません。');\n  if (products.getLastColumn() < 11) products.getRange(1,11).setValue('商品画像URL');\n  if (products.getLastColumn() < 12) products.getRange(1,12).setValue('商品メモ');\n  if (!products.getRange(1,11).getValue()) products.getRange(1,11).setValue('商品画像URL');\n  if (!products.getRange(1,12).getValue()) products.getRange(1,12).setValue('商品メモ');\n  products.autoResizeColumns(1, Math.max(12, products.getLastColumn()));\n  return 'v4への更新が完了しました。Productsシートまたは本部画面から商品画像URLと商品メモを登録できます。';\n}\n\n\n// v4からv5へ更新する時に「1回だけ」実行してください。既存データは消しません。\nfunction upgradeToV5() {\n  const ss = SpreadsheetApp.getActiveSpreadsheet();\n  const settings = ss.getSheetByName(SHEETS.SETTINGS);\n  if (!settings) throw new Error('Settingsシートが見つかりません。');\n  const vals = settings.getLastRow() > 1 ? settings.getRange(2,1,settings.getLastRow()-1,2).getValues() : [];\n  const hasFolder = vals.some(r => r[0] === '商品画像フォルダID');\n  if (!hasFolder) settings.appendRow(['商品画像フォルダID','']);\n  return 'v5への更新が完了しました。本部画面から商品画像を直接アップロードできます。初回アップロード時にGoogle Driveの権限許可が必要です。';\n}\n\n\n// v5.1からv5.2へ更新する時に「1回だけ」実行してください。\n// Webアプリから確実に同じスプレッドシートを開けるよう、スプレッドシートIDを保存します。\nfunction upgradeToV5_2() {\n  const ss = SpreadsheetApp.getActiveSpreadsheet();\n  if (!ss) throw new Error('このスクリプトを対象のスプレッドシートから開いて実行してください。');\n  PropertiesService.getScriptProperties().setProperty('SPREADSHEET_ID', ss.getId());\n  return 'v5.2への更新が完了しました。スプレッドシートIDを固定登録しました。';\n}\n\n\n// v5.7からv5.8へ更新する時に「1回だけ」実行してください。既存データは消しません。\n// 在庫単位と購入単位を分離します。\nfunction upgradeToV5_8() {\n  const ss = getSS_();\n  const sheet = ss.getSheetByName(SHEETS.PRODUCTS);\n  if (!sheet) throw new Error('Productsシートが見つかりません。');\n  sheet.getRange(1,4).setValue('在庫単位');\n  sheet.getRange(1,6).setValue('推奨発注数（購入単位）');\n  sheet.getRange(1,7).setValue('発注数ステップ');\n  sheet.getRange(1,13).setValue('1発注あたり数量');\n  sheet.getRange(1,14).setValue('発注単位名');\n  if (sheet.getLastRow() > 1) {\n    const rows = sheet.getRange(2,1,sheet.getLastRow()-1,14).getValues();\n    for (let i = 0; i < rows.length; i++) {\n      const rowNo = i + 2;\n      const id = String(rows[i][0] || '');\n      const stockUnit = String(rows[i][3] || '個');\n      let packQty = Number(rows[i][12]) || 0;\n      let purchaseUnit = String(rows[i][13] || '').trim();\n      if (!packQty) packQty = 1;\n      if (!purchaseUnit) purchaseUnit = stockUnit;\n      // レジロールは例として「1セット=10巻」に初期設定します。\n      if (id === 'P001' && (!rows[i][12] || !rows[i][13])) {\n        packQty = 10;\n        purchaseUnit = 'セット';\n        if (Number(rows[i][5]) === 10) sheet.getRange(rowNo,6).setValue(1);\n      }\n      sheet.getRange(rowNo,13,1,2).setValues([[packQty,purchaseUnit]]);\n      if (!Number(rows[i][6])) sheet.getRange(rowNo,7).setValue(1);\n    }\n  }\n  sheet.autoResizeColumns(1,14);\n  return 'v5.8への更新が完了しました。レジロールは「1セット=10巻」に設定しました。他の商品は商品マスターから購入単位を設定できます。';\n}\n\n\n// v5.10からv6.0へ更新する時に「1回だけ」実行してください。既存データは消しません。\nfunction upgradeToV6() {\n  const ss = getSS_();\n  const settings = ss.getSheetByName(SHEETS.SETTINGS);\n  if (!settings) throw new Error('Settingsシートが見つかりません。');\n  ensureSetting_(settings, 'アプリバージョン', APP_VERSION);\n  ensureSetting_(settings, '更新マニフェストURL', '');\n  ensureSetting_(settings, '最終更新確認日時', '');\n  ensureSetting_(settings, '最終更新バージョン', APP_VERSION);\n  return 'v6.0への更新が完了しました。次に新しいバージョンでデプロイし、本部発注→システム更新で更新元URLを設定してください。';\n}\n\n\n// v6.0.4からv6.0.5へ更新する時に「1回だけ」実行してください。\n// 現在存在するWEB_APPデプロイのうち、最終更新日時が最も新しいものを固定保存します。\nfunction upgradeToV6_0_5() {\n  const ss = getSS_();\n  const scriptId = ScriptApp.getScriptId();\n  const result = scriptApiRequest_('get', '/v1/projects/' + encodeURIComponent(scriptId) + '/deployments');\n  const deployments = (result && result.deployments) ? result.deployments : [];\n  const webApps = deployments.filter(function(d) {\n    return d && Array.isArray(d.entryPoints) && d.entryPoints.some(function(ep) {\n      return ep && ep.entryPointType === 'WEB_APP' && ep.webApp && ep.webApp.url;\n    });\n  });\n  if (!webApps.length) {\n    throw new Error('Webアプリのデプロイが見つかりません。先に「新しいデプロイ」→「ウェブアプリ」でデプロイしてください。');\n  }\n  webApps.sort(function(a,b) {\n    return new Date(b.updateTime || 0).getTime() - new Date(a.updateTime || 0).getTime();\n  });\n  const selected = webApps[0];\n  const webEntry = selected.entryPoints.find(function(ep) {\n    return ep && ep.entryPointType === 'WEB_APP' && ep.webApp && ep.webApp.url;\n  });\n  setSetting_(ss, 'WebアプリデプロイID', String(selected.deploymentId || ''));\n  setSetting_(ss, 'WebアプリURL', webEntry && webEntry.webApp ? String(webEntry.webApp.url || '') : '');\n  setSetting_(ss, 'アプリバージョン', APP_VERSION);\n  setSetting_(ss, '最終更新バージョン', APP_VERSION);\n  return 'v6.0.5への更新が完了しました。WebアプリデプロイIDを固定登録しました：' + selected.deploymentId;\n}\n\n// v6.4.0の商品追加を既存データを消さずに1回だけ適用します。\n// 同名商品は追加せず、既存の「コピー用紙」だけを「コピー用紙 A4」へ安全に名称変更します。\nfunction ensureV6_4_0Products_(ss) {\n  var settings = ss.getSheetByName(SHEETS.SETTINGS);\n  var products = ss.getSheetByName(SHEETS.PRODUCTS);\n  if (!settings || !products) return;\n  var currentSettings = getSettings_(ss);\n  if (String(currentSettings[V6_4_0_PRODUCT_MIGRATION_KEY] || '') === '完了') return;\n\n  var lock = LockService.getDocumentLock();\n  lock.waitLock(10000);\n  try {\n    currentSettings = getSettings_(ss);\n    if (String(currentSettings[V6_4_0_PRODUCT_MIGRATION_KEY] || '') === '完了') return;\n\n    var lastRow = products.getLastRow();\n    var values = lastRow > 1 ? products.getRange(2,1,lastRow-1,14).getValues() : [];\n    var names = {};\n    var maxNo = 0;\n    for (var i = 0; i < values.length; i++) {\n      var idMatch = String(values[i][0] || '').match(/^P(\\d+)$/i);\n      if (idMatch) maxNo = Math.max(maxNo, Number(idMatch[1]) || 0);\n      var existingName = String(values[i][2] || '').trim();\n      if (existingName === 'コピー用紙') {\n        products.getRange(i + 2,3).setValue('コピー用紙 A4');\n        existingName = 'コピー用紙 A4';\n      }\n      if (existingName) names[existingName.toLowerCase()] = true;\n    }\n\n    var rowsToAdd = [];\n    for (var j = 0; j < V6_4_0_PRODUCTS.length; j++) {\n      var category = V6_4_0_PRODUCTS[j][0];\n      var name = V6_4_0_PRODUCTS[j][1];\n      if (names[name.toLowerCase()]) continue;\n      maxNo++;\n      rowsToAdd.push([\n        'P' + Utilities.formatString('%03d', maxNo),category,name,'個',1,1,1,true,\n        '','','','',1,'個'\n      ]);\n      names[name.toLowerCase()] = true;\n    }\n    if (rowsToAdd.length) {\n      products.getRange(products.getLastRow() + 1,1,rowsToAdd.length,14).setValues(rowsToAdd);\n    }\n    setSetting_(ss, V6_4_0_PRODUCT_MIGRATION_KEY, '完了');\n  } finally {\n    lock.releaseLock();\n  }\n}\n\n// 店舗ごとの対象外商品を保持する列を、既存データを変更せず追加します。\nfunction ensureProductStoreExclusionColumn_(ss) {\n  var sheet = ss.getSheetByName(SHEETS.PRODUCTS);\n  if (!sheet) return;\n  if (String(sheet.getRange(1,15).getValue() || '').trim() !== '対象外店舗') {\n    sheet.getRange(1,15).setValue('対象外店舗');\n  }\n}\n\n// 既存の行順を初期表示順として保存し、以後は商品行を動かさずに表示順だけを管理します。\nfunction ensureProductDisplayOrderColumn_(ss) {\n  var sheet = ss.getSheetByName(SHEETS.PRODUCTS);\n  if (!sheet) return;\n  if (String(sheet.getRange(1,16).getValue() || '').trim() !== '表示順') sheet.getRange(1,16).setValue('表示順');\n  var count = Math.max(0, sheet.getLastRow() - 1);\n  if (!count) return;\n  var values = sheet.getRange(2,16,count,1).getValues();\n  var changed = false;\n  for (var i = 0; i < values.length; i++) {\n    if (!(Number(values[i][0]) > 0)) {\n      values[i][0] = i + 1;\n      changed = true;\n    }\n  }\n  if (changed) sheet.getRange(2,16,count,1).setValues(values);\n}\n\nfunction parseExcludedStores_(value) {\n  return String(value || '').split(',').map(function(s){return s.trim();}).filter(Boolean);\n}\n\nfunction getInitialData() {\n  const ss = getSS_();\n  ensureV6_4_0Products_(ss);\n  ensureProductStoreExclusionColumn_(ss);\n  ensureProductDisplayOrderColumn_(ss);\n  const settings = getSettings_(ss);\n  return {\n    appName: settings['管理アプリ名'] || '消耗品管理・発注',\n    stores: (settings['店舗一覧'] || '').split(',').map(s => s.trim()).filter(Boolean),\n    staffByStore: getStaffByStore_(settings)\n  };\n}\n\nfunction getStaffNames(store) {\n  return getStaffByStore_(getSettings_(getSS_()))[String(store || '').trim()] || [];\n}\n\nfunction saveStaffName(payload) {\n  const store = String(payload && payload.store || '').trim();\n  const name = String(payload && payload.name || '').trim().replace(/\\s+/g, ' ');\n  if (!store || !name) throw new Error('店舗と担当者名を入力してください。');\n  if (name.length > 30) throw new Error('担当者名は30文字以内で入力してください。');\n  const ss = getSS_();\n  const settings = getSettings_(ss);\n  const stores = String(settings['店舗一覧'] || '').split(',').map(function(s){return s.trim();}).filter(Boolean);\n  if (stores.indexOf(store) < 0) throw new Error('店舗を選び直してください。');\n  const staffByStore = getStaffByStore_(settings);\n  const names = staffByStore[store] || [];\n  if (names.indexOf(name) < 0) names.push(name);\n  staffByStore[store] = names.sort(function(a,b){return a.localeCompare(b, 'ja');});\n  setSetting_(ss, '担当者一覧JSON', JSON.stringify(staffByStore));\n  return {ok:true, names:staffByStore[store]};\n}\n\nfunction deleteStaffName(payload) {\n  const store = String(payload && payload.store || '').trim();\n  const name = String(payload && payload.name || '').trim();\n  if (!store || !name) throw new Error('削除対象を確認できません。');\n  const ss = getSS_();\n  const settings = getSettings_(ss);\n  const staffByStore = getStaffByStore_(settings);\n  staffByStore[store] = (staffByStore[store] || []).filter(function(item){return item !== name;});\n  setSetting_(ss, '担当者一覧JSON', JSON.stringify(staffByStore));\n  return {ok:true, names:staffByStore[store]};\n}\n\nfunction getStaffByStore_(settings) {\n  const raw = String((settings || {})['担当者一覧JSON'] || '').trim();\n  if (!raw) return {};\n  try {\n    const parsed = JSON.parse(raw);\n    const out = {};\n    Object.keys(parsed || {}).forEach(function(store){\n      out[String(store)] = (Array.isArray(parsed[store]) ? parsed[store] : []).map(function(name){return String(name || '').trim();}).filter(Boolean);\n    });\n    return out;\n  } catch (e) {\n    return {};\n  }\n}\n\nfunction getInventory(store) {\n  const ss = getSS_();\n  const normalizedStore = String(store || '').trim();\n  const cacheKey = APP_CACHE_KEYS.INVENTORY_PREFIX + encodeURIComponent(normalizedStore);\n  const cached = getCachedJson_(cacheKey);\n  if (cached) return cached;\n  const products = getProducts_(ss, normalizedStore);\n  const sheet = ss.getSheetByName(SHEETS.INVENTORY);\n  const values = sheet && sheet.getLastRow() > 1 ? sheet.getRange(2,1,sheet.getLastRow()-1,6).getValues() : [];\n  const latest = {};\n  values.forEach(r => { if (String(r[1] || '').trim() === normalizedStore) latest[r[2]] = Number(r[4]) || 0; });\n\n  const orderSheet = ss.getSheetByName(SHEETS.ORDERS);\n  const orderValues = orderSheet && orderSheet.getLastRow() > 1 ? orderSheet.getRange(2,1,orderSheet.getLastRow()-1,11).getValues() : [];\n  const lastOrders = {};\n  orderValues.forEach(function(r) {\n    if (String(r[2] || '').trim() !== normalizedStore) return;\n    const productId = String(r[4] || '').trim();\n    if (!productId) return;\n    const timestamp = r[1] instanceof Date ? r[1].getTime() : (new Date(r[1]).getTime() || 0);\n    if (!lastOrders[productId] || timestamp >= lastOrders[productId].timestamp) {\n      lastOrders[productId] = {\n        timestamp:timestamp,\n        date:r[1] instanceof Date ? Utilities.formatDate(r[1], Session.getScriptTimeZone(), 'yyyy/MM/dd HH:mm') : String(r[1] || ''),\n        qty:Number(r[6]) || 0,\n        unit:String(r[7] || '')\n      };\n    }\n  });\n\n  const result = products.map(function(p) {\n    const last = lastOrders[p.id] || null;\n    return {\n      ...p,\n      currentStock:latest[p.id] ?? 0,\n      lastOrderDate:last ? last.date : '',\n      lastOrderQty:last ? last.qty : 0,\n      lastOrderUnit:last ? last.unit : ''\n    };\n  });\n  putCachedJson_(cacheKey, result, 60);\n  return result;\n}\n\nfunction saveInventoryItem(payload) {\n  if (!payload || !payload.store || !payload.productId) throw new Error('入力内容が不正です。');\n  const ss = getSS_();\n  ensureProductStoreExclusionColumn_(ss);\n  const eligible = getProducts_(ss, String(payload.store).trim()).some(function(p){return String(p.id) === String(payload.productId);});\n  if (!eligible) throw new Error('この商品は選択中の店舗では対象外です。画面を再読み込みしてください。');\n  const sheet = ss.getSheetByName(SHEETS.INVENTORY);\n  if (!sheet) throw new Error('Inventoryシートが見つかりません。');\n  sheet.appendRow([new Date(),payload.store,payload.productId,payload.productName||'',Number(payload.currentStock)||0,payload.user||'']);\n  clearInventoryCache_(payload.store);\n  return {ok:true};\n}\n\nfunction submitOrder(payload) {\n  if (!payload || !payload.store || !Array.isArray(payload.items) || payload.items.length === 0) throw new Error('発注商品がありません。');\n  const ss = getSS_();\n  const settings = getSettings_(ss);\n  const allowedStores = String(settings['店舗一覧'] || '').split(',').map(s => s.trim()).filter(Boolean);\n  if (allowedStores.indexOf(String(payload.store).trim()) < 0) throw new Error('選択された店舗を確認できません。店舗を選び直してください。');\n  ensureProductStoreExclusionColumn_(ss);\n  const eligibleProducts = {};\n  getProducts_(ss, String(payload.store).trim()).forEach(function(p){eligibleProducts[String(p.id)] = true;});\n  for (var itemIndex = 0; itemIndex < payload.items.length; itemIndex++) {\n    if (!eligibleProducts[String(payload.items[itemIndex].productId || '')]) {\n      throw new Error('発注対象外の商品が含まれています。画面を再読み込みして発注内容を確認してください。');\n    }\n  }\n  const recipient = settings['本部発注メール'];\n  if (!recipient || recipient === 'head-office@example.com') throw new Error('Settingsシートの「本部発注メール」を実際のアドレスに変更してください。');\n  const orderId = Utilities.formatDate(new Date(), Session.getScriptTimeZone(), 'yyyyMMdd-HHmmss') + '-' + Math.floor(Math.random()*900+100);\n  const now = new Date();\n  const sheet = ss.getSheetByName(SHEETS.ORDERS);\n  const rows = payload.items.map(i => [orderId,now,payload.store,payload.user||'',i.productId,i.productName,Number(i.orderQty)||0,i.orderUnitName||i.unit||'','発注依頼',recipient,payload.note||'']);\n  sheet.getRange(sheet.getLastRow()+1,1,rows.length,11).setValues(rows);\n  clearInventoryCache_(payload.store);\n\n  const subject = `${settings['メール件名接頭辞'] || '【消耗品発注依頼】'}${payload.store}`;\n  const lines = [`${payload.store}から消耗品の発注依頼です。`,'',`発注ID：${orderId}`,`発注者：${payload.user || '未入力'}`,`発注日時：${Utilities.formatDate(now, Session.getScriptTimeZone(), 'yyyy/MM/dd HH:mm')}`,'','【発注内容】'];\n  payload.items.forEach(i => {\n    const purchaseUnit = i.orderUnitName || i.unit || '';\n    const packQty = Math.max(1, Number(i.packQty) || 1);\n    const stockUnit = i.stockUnit || '';\n    const total = (Number(i.orderQty) || 0) * packQty;\n    const detail = packQty > 1 || purchaseUnit !== stockUnit ? `（合計 ${total}${stockUnit} / 1${purchaseUnit}=${packQty}${stockUnit}）` : '';\n    lines.push(`・${i.productName} × ${i.orderQty}${purchaseUnit}${detail}`);\n  });\n  if (payload.note) lines.push('','【備考】',payload.note);\n  MailApp.sendEmail({to:recipient,subject,body:lines.join('\\n')});\n  return {ok:true,orderId};\n}\n\nfunction getOrderHistory(store) {\n  const ss = getSS_();\n  const sheet = ss.getSheetByName(SHEETS.ORDERS);\n  if (!sheet || sheet.getLastRow() <= 1) return [];\n  ensureOrdersDeliveryColumns_(sheet);\n  const values = sheet.getRange(2,1,sheet.getLastRow()-1,16).getValues();\n  const normalizedStore = String(store || '').trim();\n  return values\n    .map((r, index) => ({row:r, rowNumber:index + 2}))\n    .filter(item => !normalizedStore || String(item.row[2] || '').trim() === normalizedStore)\n    .slice(-100)\n    .reverse()\n    .map(item => {\n      const r = item.row;\n      return {\n      rowNumber:item.rowNumber,\n      orderId:r[0],\n      date:formatDateTimeJa_(r[1]),\n      store:String(r[2] || '').trim(),\n      user:r[3],\n      productName:r[5],\n      qty:r[6],\n      unit:r[7],\n      status:r[8],\n      deliveryFrom:formatDateInputValue_(r[11]),\n      deliveryTo:formatDateInputValue_(r[12]),\n      arrangementType:String(r[13] || '').trim(),\n      arrangementNote:String(r[14] || '').trim(),\n      arrangementDetail:String(r[15] || '').trim()\n    };\n    });\n}\n\nfunction cancelOrderLine(payload) {\n  const row = Number(payload && payload.rowNumber);\n  const store = String((payload && payload.store) || '').trim();\n  const orderId = String((payload && payload.orderId) || '').trim();\n  if (!row || row < 2 || !store || !orderId) throw new Error('取り下げ対象を確認できません。');\n\n  const lock = LockService.getDocumentLock();\n  lock.waitLock(10000);\n  try {\n    const sheet = getSS_().getSheetByName(SHEETS.ORDERS);\n    if (!sheet || row > sheet.getLastRow()) throw new Error('対象の発注依頼が見つかりません。');\n    const values = sheet.getRange(row,1,1,9).getValues()[0];\n    if (String(values[0] || '').trim() !== orderId || String(values[2] || '').trim() !== store) {\n      throw new Error('対象の発注依頼が一致しません。履歴を再読み込みしてください。');\n    }\n    if (String(values[8] || '').trim() !== '発注依頼') {\n      throw new Error('この発注はすでに処理されているため取り下げできません。');\n    }\n    sheet.getRange(row,9).setValue('キャンセル');\n    SpreadsheetApp.flush();\n    clearInventoryCache_(store);\n    return {ok:true, status:'キャンセル'};\n  } finally {\n    lock.releaseLock();\n  }\n}\n\nfunction verifyAdminPin(pin) {\n  assertAdmin_(pin);\n  return {ok:true};\n}\n\nfunction getHeadOfficeOrders(pin) {\n  assertAdmin_(pin);\n  const ss = getSS_();\n  const products = {};\n  getProducts_(ss).forEach(p => products[p.id] = p);\n  const sheet = ss.getSheetByName(SHEETS.ORDERS);\n  if (!sheet || sheet.getLastRow() <= 1) return [];\n  ensureOrdersDeliveryColumns_(sheet);\n  const rows = sheet.getRange(2,1,sheet.getLastRow()-1,16).getValues();\n  return rows.map((r,index) => ({row:r,rowNumber:index + 2})).reverse().filter(item => String(item.row[8] || '').trim() !== 'キャンセル').map(item => {\n    const r = item.row;\n    const p = products[r[4]] || {};\n    return {\n      rowNumber:item.rowNumber,\n      orderId:r[0],\n      date:r[1] instanceof Date ? Utilities.formatDate(r[1], Session.getScriptTimeZone(), 'yyyy/MM/dd HH:mm') : String(r[1] || ''),\n      store:r[2], user:r[3], productId:r[4], productName:r[5], qty:r[6], unit:r[7], status:r[8], note:r[10],\n      deliveryFrom:formatDateInputValue_(r[11]), deliveryTo:formatDateInputValue_(r[12]),\n      arrangementType:String(r[13] || '').trim(), arrangementNote:String(r[14] || '').trim(), arrangementDetail:String(r[15] || '').trim(),\n      supplier:p.supplier || '', orderUrl:p.orderUrl || '', imageUrl:p.imageUrl || '', productMemo:p.productMemo || '', packQty:p.packQty || 1, stockUnit:p.unit || '', orderUnitName:p.orderUnitName || p.unit || ''\n    };\n  });\n}\n\nfunction updateOrderLineStatus(payload) {\n  assertAdmin_(payload && payload.pin);\n  const allowed = ['発注依頼','手配済み'];\n  if (!payload || !allowed.includes(payload.status)) throw new Error('不正な状態です。');\n  const row = Number(payload.rowNumber);\n  const lock = LockService.getDocumentLock();\n  lock.waitLock(10000);\n  try {\n    const ss = getSS_();\n    const sheet = ss.getSheetByName(SHEETS.ORDERS);\n    ensureOrdersDeliveryColumns_(sheet);\n    if (!row || row < 2 || row > sheet.getLastRow()) throw new Error('対象行が見つかりません。');\n    const currentStatus = String(sheet.getRange(row,9).getValue() || '').trim();\n    if (payload.status === '手配済み' && currentStatus !== '発注依頼') {\n      throw new Error('この発注はすでに処理済みか、取り下げられています。画面を更新してください。');\n    }\n    if (payload.status === '手配済み') {\n      const deliveryUndecided = payload.deliveryUndecided === true;\n      const deliveryFrom = String(payload.deliveryFrom || '').trim();\n      const deliveryTo = String(payload.deliveryTo || deliveryFrom).trim();\n      if (!deliveryUndecided && (!/^\\d{4}-\\d{2}-\\d{2}$/.test(deliveryFrom) || !/^\\d{4}-\\d{2}-\\d{2}$/.test(deliveryTo))) {\n        throw new Error('納品予定日を入力してください。');\n      }\n      if (!deliveryUndecided && deliveryTo < deliveryFrom) throw new Error('納品予定の終了日は開始日以降にしてください。');\n      sheet.getRange(row,12,1,2).setValues([[deliveryUndecided ? '未確定' : deliveryFrom,deliveryUndecided ? '' : deliveryTo]]);\n      const arrangementType = String(payload.arrangementType || '').trim();\n      const allowedTypes = ['','売り切れのため代替商品へ変更','送料を考慮して数量を変更'];\n      if (!allowedTypes.includes(arrangementType)) throw new Error('手配内容が不正です。');\n      const arrangementNote = arrangementType ? '手配内容：' + arrangementType : '';\n      const arrangementDetail = String(payload.arrangementDetail || '').trim().slice(0,500);\n      sheet.getRange(row,14,1,3).setValues([[arrangementType,arrangementNote,arrangementDetail]]);\n    }\n    sheet.getRange(row,9).setValue(payload.status);\n    SpreadsheetApp.flush();\n    clearInventoryCache_(sheet.getRange(row,3).getValue());\n    return {ok:true};\n  } finally {\n    lock.releaseLock();\n  }\n}\n\nfunction ensureOrdersDeliveryColumns_(sheet) {\n  if (!sheet) throw new Error('Ordersシートが見つかりません。');\n  if (!sheet.getRange(1,12).getValue()) sheet.getRange(1,12).setValue('納品予定開始日');\n  if (!sheet.getRange(1,13).getValue()) sheet.getRange(1,13).setValue('納品予定終了日');\n  if (!sheet.getRange(1,14).getValue()) sheet.getRange(1,14).setValue('手配内容');\n  if (!sheet.getRange(1,15).getValue()) sheet.getRange(1,15).setValue('手配メモ');\n  if (!sheet.getRange(1,16).getValue()) sheet.getRange(1,16).setValue('手配補足');\n}\n\nfunction formatDateInputValue_(value) {\n  if (value instanceof Date) return Utilities.formatDate(value, Session.getScriptTimeZone(), 'yyyy-MM-dd');\n  const text = String(value || '').trim();\n  const match = text.match(/^(\\d{4})[\\/-](\\d{1,2})[\\/-](\\d{1,2})/);\n  return match ? match[1] + '-' + ('0' + match[2]).slice(-2) + '-' + ('0' + match[3]).slice(-2) : text;\n}\n\nfunction formatDateTimeJa_(value) {\n  const date = value instanceof Date ? value : new Date(value);\n  if (isNaN(date.getTime())) return String(value || '');\n  const base = Utilities.formatDate(date, Session.getScriptTimeZone(), 'yyyy/MM/dd');\n  const time = Utilities.formatDate(date, Session.getScriptTimeZone(), 'HH:mm');\n  const weekday = ['日','月','火','水','木','金','土'][Number(Utilities.formatDate(date, Session.getScriptTimeZone(), 'u')) % 7];\n  return base + '（' + weekday + '） ' + time;\n}\n\n\nfunction getProductAdminMaster(pin) {\n  assertAdmin_(pin);\n  var ss = getSS_();\n  ensureV6_4_0Products_(ss);\n  ensureProductStoreExclusionColumn_(ss);\n  ensureProductDisplayOrderColumn_(ss);\n  return getAllProducts_(ss);\n}\n\nfunction addProduct(payload) {\n  assertAdmin_(payload && payload.pin);\n  if (!payload) throw new Error('入力内容が不正です。');\n  var category = String(payload.category || '').trim();\n  var name = String(payload.name || '').trim();\n  var unit = String(payload.unit || '').trim();\n  if (!category) throw new Error('カテゴリを入力してください。');\n  if (!name) throw new Error('商品名を入力してください。');\n  if (!unit) throw new Error('単位を入力してください。');\n  var minStock = Math.max(0, Number(payload.minStock) || 0);\n  var suggestedQty = Math.max(1, Number(payload.suggestedQty) || 1);\n  var orderUnit = 1;\n  var packQty = Math.max(1, Number(payload.packQty) || 1);\n  var orderUnitName = String(payload.orderUnitName || '').trim() || unit;\n  var ss = getSS_();\n  var sheet = ss.getSheetByName(SHEETS.PRODUCTS);\n  if (!sheet) throw new Error('Productsシートが見つかりません。');\n  var all = getAllProducts_(ss);\n  for (var i = 0; i < all.length; i++) {\n    if (String(all[i].name).toLowerCase() === name.toLowerCase()) throw new Error('同じ商品名がすでに登録されています。');\n  }\n  var maxNo = 0;\n  for (var j = 0; j < all.length; j++) {\n    var m = String(all[j].id || '').match(/^P(\\d+)$/i);\n    if (m) maxNo = Math.max(maxNo, Number(m[1]) || 0);\n  }\n  var id = 'P' + Utilities.formatString('%03d', maxNo + 1);\n  ensureProductDisplayOrderColumn_(ss);\n  sheet.appendRow([id, category, name, unit, minStock, suggestedQty, orderUnit, true, '', '', '', '', packQty, orderUnitName, '', all.length + 1]);\n  clearProductCache_();\n  return {ok:true, productId:id};\n}\n\nfunction updateProductMaster(payload) {\n  assertAdmin_(payload && payload.pin);\n  if (!payload || !payload.productId) throw new Error('商品が指定されていません。');\n  var category = String(payload.category || '').trim();\n  var name = String(payload.name || '').trim();\n  var unit = String(payload.unit || '').trim();\n  if (!category || !name || !unit) throw new Error('カテゴリ・商品名・単位は必須です。');\n  var ss = getSS_();\n  var sheet = ss.getSheetByName(SHEETS.PRODUCTS);\n  if (!sheet) throw new Error('Productsシートが見つかりません。');\n  var lastRow = sheet.getLastRow();\n  if (lastRow <= 1) throw new Error('商品が見つかりません。');\n  var rows = sheet.getRange(2,1,lastRow-1,12).getValues();\n  var idx = -1;\n  for (var i = 0; i < rows.length; i++) if (String(rows[i][0]) === String(payload.productId)) { idx = i; break; }\n  if (idx < 0) throw new Error('商品が見つかりません。');\n  var rowNo = idx + 2;\n  var supplier = String(payload.supplier || '').trim();\n  var orderUrl = String(payload.orderUrl || '').trim();\n  var imageUrl = String(payload.imageUrl || '').trim();\n  var productMemo = String(payload.productMemo || '').trim();\n  if (orderUrl && !/^https?:\\/\\//i.test(orderUrl)) throw new Error('発注ページURLは http:// または https:// から入力してください。');\n  if (imageUrl && !/^https?:\\/\\//i.test(imageUrl)) throw new Error('商品画像URLは http:// または https:// から入力してください。');\n  sheet.getRange(rowNo,2,1,7).setValues([[\n    category,\n    name,\n    unit,\n    Math.max(0, Number(payload.minStock) || 0),\n    Math.max(1, Number(payload.suggestedQty) || 1),\n    1,\n    payload.active === false ? false : true\n  ]]);\n  var packQty = Math.max(1, Number(payload.packQty) || 1);\n  var orderUnitName = String(payload.orderUnitName || '').trim() || unit;\n  sheet.getRange(rowNo,13,1,2).setValues([[packQty,orderUnitName]]);\n  sheet.getRange(rowNo,9,1,4).setValues([[supplier,orderUrl,imageUrl,productMemo]]);\n  if (Object.prototype.hasOwnProperty.call(payload,'excludedStores')) {\n    var allowedStores = String(getSettings_(ss)['店舗一覧'] || '').split(',').map(function(s){return s.trim();}).filter(Boolean);\n    var requestedExcluded = Array.isArray(payload.excludedStores) ? payload.excludedStores : [];\n    var excludedStores = [];\n    for (var e = 0; e < requestedExcluded.length; e++) {\n      var excludedStore = String(requestedExcluded[e] || '').trim();\n      if (excludedStore && allowedStores.indexOf(excludedStore) >= 0 && excludedStores.indexOf(excludedStore) < 0) excludedStores.push(excludedStore);\n    }\n    sheet.getRange(rowNo,15).setValue(excludedStores.join(','));\n  }\n  clearProductCache_();\n  return {ok:true};\n}\n\nfunction updateProductDisplayOrder(payload) {\n  assertAdmin_(payload && payload.pin);\n  var order = payload && Array.isArray(payload.productIds) ? payload.productIds.map(function(id){return String(id || '').trim();}).filter(Boolean) : [];\n  var lock = LockService.getDocumentLock();\n  lock.waitLock(10000);\n  try {\n    var ss = getSS_();\n    ensureProductDisplayOrderColumn_(ss);\n    var sheet = ss.getSheetByName(SHEETS.PRODUCTS);\n    var rows = sheet.getLastRow() > 1 ? sheet.getRange(2,1,sheet.getLastRow()-1,1).getValues().map(function(r){return String(r[0] || '').trim();}).filter(Boolean) : [];\n    if (order.length !== rows.length || order.some(function(id){return rows.indexOf(id) < 0;}) || (new Set(order)).size !== order.length) {\n      throw new Error('商品一覧が更新されています。画面を更新してから並び順を保存してください。');\n    }\n    var positionById = {};\n    for (var i = 0; i < order.length; i++) positionById[order[i]] = i + 1;\n    sheet.getRange(2,16,rows.length,1).setValues(rows.map(function(id){return [positionById[id]];}));\n    SpreadsheetApp.flush();\n    clearProductCache_();\n    return {ok:true};\n  } finally {\n    lock.releaseLock();\n  }\n}\n\nfunction getAllProducts_(ss) {\n  const cached = getCachedJson_(APP_CACHE_KEYS.PRODUCTS);\n  if (cached) return cached;\n  var sheet = ss.getSheetByName(SHEETS.PRODUCTS);\n  if (!sheet || sheet.getLastRow() <= 1) return [];\n  var cols = Math.max(16, sheet.getLastColumn());\n  var values = sheet.getRange(2,1,sheet.getLastRow()-1,cols).getValues();\n  var out = [];\n  for (var i = 0; i < values.length; i++) {\n    var r = values[i];\n    if (!r[0]) continue;\n    out.push({\n      id:r[0], category:r[1], name:r[2], unit:r[3], minStock:Number(r[4])||0,\n      suggestedQty:Number(r[5])||1, orderUnit:Number(r[6])||1,\n      active:(r[7]===true || String(r[7]).toUpperCase()==='TRUE'),\n      supplier:r[8]||'', orderUrl:r[9]||'', imageUrl:r[10]||'', productMemo:r[11]||'',\n      packQty:Number(r[12])||1, orderUnitName:r[13]||r[3]||'',\n      excludedStores:parseExcludedStores_(r[14]), displayOrder:Number(r[15]) || (i + 1)\n    });\n  }\n  out.sort(function(a,b){return a.displayOrder - b.displayOrder;});\n  putCachedJson_(APP_CACHE_KEYS.PRODUCTS, out, 60);\n  return out;\n}\n\nfunction getSupplierMaster(pin) {\n  assertAdmin_(pin);\n  return getProducts_(getSS_());\n}\n\nfunction saveSupplierLink(payload) {\n  assertAdmin_(payload && payload.pin);\n  if (!payload || !payload.productId) throw new Error('商品が指定されていません。');\n  const url = String(payload.orderUrl || '').trim();\n  const imageUrl = String(payload.imageUrl || '').trim();\n  if (url && !/^https?:\\/\\//i.test(url)) throw new Error('発注ページURLは http:// または https:// から入力してください。');\n  if (imageUrl && !/^https?:\\/\\//i.test(imageUrl)) throw new Error('商品画像URLは http:// または https:// から入力してください。');\n  const ss = getSS_();\n  const sheet = ss.getSheetByName(SHEETS.PRODUCTS);\n  const rows = sheet.getRange(2,1,sheet.getLastRow()-1,Math.max(12,sheet.getLastColumn())).getValues();\n  const idx = rows.findIndex(r => r[0] === payload.productId);\n  if (idx < 0) throw new Error('商品が見つかりません。');\n  sheet.getRange(idx+2,9,1,4).setValues([[String(payload.supplier||'').trim(),url,imageUrl,String(payload.productMemo||'').trim()]]);\n  clearProductCache_();\n  return {ok:true};\n}\n\n\n\nfunction uploadProductImage(payload) {\n  assertAdmin_(payload && payload.pin);\n  if (!payload || !payload.productId) throw new Error('商品が指定されていません。');\n  const dataUrl = String(payload.dataUrl || '');\n  const m = dataUrl.match(/^data:(image\\/[a-zA-Z0-9.+-]+);base64,(.+)$/);\n  if (!m) throw new Error('画像データの形式が不正です。');\n  const mimeType = m[1];\n  const bytes = Utilities.base64Decode(m[2]);\n  if (bytes.length > 5 * 1024 * 1024) throw new Error('画像サイズは5MB以下にしてください。');\n  const ss = getSS_();\n  const product = getProducts_(ss).find(p => p.id === payload.productId);\n  if (!product) throw new Error('商品が見つかりません。');\n  const folder = getOrCreateProductImageFolder_(ss);\n  const ext = ({'image/jpeg':'jpg','image/png':'png','image/webp':'webp','image/gif':'gif'})[mimeType] || 'bin';\n  const safeName = sanitizeFileName_(product.name || payload.productId);\n  const fileName = `${payload.productId}_${safeName}_${Utilities.formatDate(new Date(), Session.getScriptTimeZone(), 'yyyyMMdd_HHmmss')}.${ext}`;\n  const blob = Utilities.newBlob(bytes, mimeType, fileName);\n  const file = folder.createFile(blob);\n  file.setSharing(DriveApp.Access.ANYONE_WITH_LINK, DriveApp.Permission.VIEW);\n  const imageUrl = `https://drive.google.com/file/d/${file.getId()}/view?usp=sharing`;\n  const sheet = ss.getSheetByName(SHEETS.PRODUCTS);\n  const rows = sheet.getRange(2,1,sheet.getLastRow()-1,Math.max(12,sheet.getLastColumn())).getValues();\n  const idx = rows.findIndex(r => r[0] === payload.productId);\n  if (idx < 0) throw new Error('商品が見つかりません。');\n  sheet.getRange(idx+2,11).setValue(imageUrl);\n  clearProductCache_();\n  return {ok:true, imageUrl, fileId:file.getId()};\n}\n\nfunction getOrCreateProductImageFolder_(ss) {\n  const settingsSheet = ss.getSheetByName(SHEETS.SETTINGS);\n  const values = settingsSheet.getRange(2,1,Math.max(0, settingsSheet.getLastRow()-1),2).getValues();\n  let rowIndex = values.findIndex(r => r[0] === '商品画像フォルダID');\n  let folderId = rowIndex >= 0 ? String(values[rowIndex][1] || '').trim() : '';\n  if (folderId) {\n    try {\n      return DriveApp.getFolderById(folderId);\n    } catch (e) {}\n  }\n  const rootFolderName = '消耗品管理・発注';\n  const childFolderName = '商品画像';\n  let rootFolder;\n  const rootIt = DriveApp.getFoldersByName(rootFolderName);\n  rootFolder = rootIt.hasNext() ? rootIt.next() : DriveApp.createFolder(rootFolderName);\n  let childFolder;\n  const childIt = rootFolder.getFoldersByName(childFolderName);\n  childFolder = childIt.hasNext() ? childIt.next() : rootFolder.createFolder(childFolderName);\n  childFolder.setSharing(DriveApp.Access.ANYONE_WITH_LINK, DriveApp.Permission.VIEW);\n  if (rowIndex >= 0) {\n    settingsSheet.getRange(rowIndex+2,2).setValue(childFolder.getId());\n  } else {\n    settingsSheet.appendRow(['商品画像フォルダID', childFolder.getId()]);\n  }\n  return childFolder;\n}\n\nfunction sanitizeFileName_(name) {\n  return String(name || '').replace(/[\\\\/:*?\"<>|#%&{}$!'@+=`~]/g, '_').replace(/\\s+/g, '_').slice(0, 80);\n}\n\n// __FUNFIELDS_SELF_UPDATER_V1__\nfunction getSystemUpdateInfo(pin) {\n  const ss = getSS_();\n  const settings = getSettings_(ss);\n  return {\n    currentVersion: APP_VERSION,\n    lastChecked: settings['最終更新確認日時'] || '',\n    lastVersion: settings['最終更新バージョン'] || APP_VERSION\n  };\n}\n\nfunction getAppVersionForReload(pin) {\n  // v6.2.0以前の画面から更新した場合も、HTML本体が配信可能になるまで再読み込みさせません。\n  const readiness = getWebAppReadiness(APP_VERSION);\n  return {version:readiness.ready ? APP_VERSION : '', ready:readiness.ready};\n}\n\nfunction getWebAppReadiness(targetVersion) {\n  const ss = getSS_();\n  const settings = getSettings_(ss);\n  const serviceUrl = String(settings['WebアプリURL'] || ScriptApp.getService().getUrl() || '').trim();\n  if (!serviceUrl) return {ready:false, status:0, serviceUrl:''};\n  const checkUrl = serviceUrl + (serviceUrl.indexOf('?') >= 0 ? '&' : '?') + '__ready=' + Date.now();\n  try {\n    const response = UrlFetchApp.fetch(checkUrl, {muteHttpExceptions:true, followRedirects:true});\n    const body = response.getContentText();\n    const marker = 'data-app-version=\"' + String(targetVersion || '') + '\"';\n    return {ready:response.getResponseCode() === 200 && body.indexOf(marker) >= 0, status:response.getResponseCode(), serviceUrl:serviceUrl};\n  } catch (e) {\n    return {ready:false, status:0, serviceUrl:serviceUrl};\n  }\n}\n\nfunction saveUpdateManifestUrl(payload) {\n  assertAdmin_(payload && payload.pin);\n  const url = String((payload && payload.url) || '').trim() || DEFAULT_UPDATE_MANIFEST_URL;\n  if (!/^https:\\/\\//i.test(url)) throw new Error('更新マニフェストURLは https:// から始まるURLを設定してください。');\n  const ss = getSS_();\n  persistUpdateManifestUrl_(ss, url);\n  return {ok:true, url:url};\n}\n\nfunction checkForAppUpdate(pin) {\n  const ss = getSS_();\n  const url = DEFAULT_UPDATE_MANIFEST_URL;\n  const manifest = fetchJson_(url, '更新マニフェスト');\n  validateReleaseManifest_(manifest);\n  const hasUpdate = compareVersions_(String(manifest.version), APP_VERSION) > 0;\n  setSetting_(ss, '最終更新確認日時', new Date());\n  setSetting_(ss, '最終更新バージョン', String(manifest.version));\n  return {\n    ok:true,\n    currentVersion:APP_VERSION,\n    latestVersion:String(manifest.version),\n    hasUpdate:hasUpdate,\n    notes:manifest.notes || '',\n    publishedAt:manifest.publishedAt || '',\n    packageUrl:String(manifest.packageUrl || '')\n  };\n}\n\nfunction applyAppUpdate(payload) {\n  const ss = getSS_();\n  const settings = getSettings_(ss);\n  // 発注者向け更新では、任意コードの取り込みを防ぐため正式なGitHub更新元だけを使用します。\n  const manifestUrl = persistUpdateManifestUrl_(ss, DEFAULT_UPDATE_MANIFEST_URL);\n\n  const manifest = fetchJson_(manifestUrl, '更新マニフェスト');\n  validateReleaseManifest_(manifest);\n  const targetVersion = String(manifest.version);\n  if (compareVersions_(targetVersion, APP_VERSION) <= 0) throw new Error('現在のバージョンが最新です。');\n\n  const packageData = fetchJson_(String(manifest.packageUrl), '更新パッケージ');\n  validateUpdatePackage_(packageData, targetVersion);\n\n  const scriptId = ScriptApp.getScriptId();\n  const deploymentId = String(settings['WebアプリデプロイID'] || '').trim();\n  if (!deploymentId) {\n    throw new Error('WebアプリデプロイIDが未登録です。Apps Scriptで upgradeToV6_0_5() を1回実行してください。');\n  }\n\n  // 更新対象が本当にWebアプリかを毎回検証し、ライブラリ等への誤更新を防ぎます。\n  const targetDeployment = scriptApiRequest_('get',\n    '/v1/projects/' + encodeURIComponent(scriptId) + '/deployments/' + encodeURIComponent(deploymentId)\n  );\n  const isWebApp = targetDeployment && Array.isArray(targetDeployment.entryPoints) &&\n    targetDeployment.entryPoints.some(function(ep) {\n      return ep && ep.entryPointType === 'WEB_APP' && ep.webApp && ep.webApp.url;\n    });\n  if (!isWebApp) {\n    throw new Error('固定登録されたデプロイIDがWebアプリではありません。安全のため更新を停止しました。upgradeToV6_0_5() を再実行してください。');\n  }\n\n  // 現在のコードをGoogle Driveへバックアップしてから更新します。\n  const currentContent = scriptApiRequest_('get', '/v1/projects/' + encodeURIComponent(scriptId) + '/content');\n  const backupFile = saveUpdateBackup_(currentContent, APP_VERSION);\n\n  try {\n    // ソース差し替えで実行が切り替わっても失われないよう、更新前に二重保存します。\n    persistUpdateManifestUrl_(ss, manifestUrl);\n    scriptApiRequest_('put', '/v1/projects/' + encodeURIComponent(scriptId) + '/content', {files:packageData.files});\n    const version = scriptApiRequest_('post', '/v1/projects/' + encodeURIComponent(scriptId) + '/versions', {\n      description:'消耗品管理・発注 v' + targetVersion + ' 自動更新'\n    });\n    const versionNumber = Number(version.versionNumber);\n    if (!versionNumber) throw new Error('新しいApps Scriptバージョン番号を取得できませんでした。');\n\n    scriptApiRequest_('put', '/v1/projects/' + encodeURIComponent(scriptId) + '/deployments/' + encodeURIComponent(deploymentId), {\n      deploymentConfig:{\n        scriptId:scriptId,\n        versionNumber:versionNumber,\n        manifestFileName:'appsscript',\n        description:'消耗品管理・発注 v' + targetVersion\n      }\n    });\n    persistUpdateManifestUrl_(ss, manifestUrl);\n    setSetting_(ss, 'アプリバージョン', targetVersion);\n    setSetting_(ss, '最終更新バージョン', targetVersion);\n    return {\n      ok:true,\n      version:targetVersion,\n      versionNumber:versionNumber,\n      backupFileName:backupFile.getName(),\n      message:'v' + targetVersion + 'への更新が完了しました。数秒待ってから画面を再読み込みしてください。'\n    };\n  } catch (e) {\n    throw new Error('自動更新に失敗しました。更新前バックアップ「' + backupFile.getName() + '」は保存済みです。詳細: ' + errorMessage_(e));\n  }\n}\n\nfunction scriptApiRequest_(method, path, body) {\n  const url = 'https://script.googleapis.com' + path;\n  const options = {\n    method:method,\n    muteHttpExceptions:true,\n    headers:{Authorization:'Bearer ' + ScriptApp.getOAuthToken()},\n    contentType:'application/json'\n  };\n  if (body !== undefined) options.payload = JSON.stringify(body);\n  const response = UrlFetchApp.fetch(url, options);\n  const code = response.getResponseCode();\n  const text = response.getContentText();\n  let data = {};\n  try { data = text ? JSON.parse(text) : {}; } catch (e) { data = {raw:text}; }\n  if (code < 200 || code >= 300) {\n    let msg = (data && data.error && data.error.message) ? data.error.message : text;\n    if (code === 403 && /SERVICE_DISABLED|has not been used|disabled/i.test(msg || '')) {\n      msg += '。Google Cloud側で「Apps Script API (script.googleapis.com)」を有効にしてください。';\n    }\n    throw new Error('Apps Script APIエラー ' + code + ': ' + msg);\n  }\n  return data;\n}\n\nfunction fetchJson_(url, label) {\n  if (!/^https:\\/\\//i.test(String(url || ''))) throw new Error(label + 'のURLが不正です。');\n  const response = UrlFetchApp.fetch(url, {muteHttpExceptions:true, followRedirects:true});\n  const code = response.getResponseCode();\n  if (code < 200 || code >= 300) throw new Error(label + 'を取得できませんでした（HTTP ' + code + '）。');\n  try { return JSON.parse(response.getContentText()); }\n  catch (e) { throw new Error(label + 'が正しいJSONではありません。'); }\n}\n\nfunction validateReleaseManifest_(m) {\n  if (!m || !m.version || !m.packageUrl) throw new Error('更新マニフェストに version または packageUrl がありません。');\n  if (!/^https:\\/\\//i.test(String(m.packageUrl))) throw new Error('更新パッケージURLは https:// である必要があります。');\n}\n\nfunction validateUpdatePackage_(p, version) {\n  if (!p || String(p.version || '') !== String(version) || !Array.isArray(p.files) || !p.files.length) {\n    throw new Error('更新パッケージの形式またはバージョンが一致しません。');\n  }\n  const names = {};\n  p.files.forEach(function(f){ if (f && f.name) names[f.name] = true; });\n  if (!names.Code || !names.Index || !names.appsscript) throw new Error('更新パッケージに Code / Index / appsscript が揃っていません。');\n  const codeFile = p.files.find(function(f){return f.name === 'Code';});\n  if (!codeFile || String(codeFile.source || '').indexOf('__FUNFIELDS_SELF_UPDATER_V1__') < 0) {\n    throw new Error('安全のため更新を停止しました。更新パッケージに自動更新機能が含まれていません。');\n  }\n}\n\nfunction saveUpdateBackup_(content, version) {\n  const root = getOrCreateNamedFolder_('消耗品管理・発注');\n  const folder = getOrCreateChildFolder_(root, 'システム更新バックアップ');\n  const stamp = Utilities.formatDate(new Date(), Session.getScriptTimeZone(), 'yyyyMMdd_HHmmss');\n  return folder.createFile('backup_v' + version + '_' + stamp + '.json', JSON.stringify(content, null, 2), MimeType.PLAIN_TEXT);\n}\n\nfunction getOrCreateNamedFolder_(name) {\n  const it = DriveApp.getFoldersByName(name);\n  return it.hasNext() ? it.next() : DriveApp.createFolder(name);\n}\n\nfunction getOrCreateChildFolder_(parent, name) {\n  const it = parent.getFoldersByName(name);\n  return it.hasNext() ? it.next() : parent.createFolder(name);\n}\n\nfunction getCurrentDeploymentId_() {\n  const url = ScriptApp.getService().getUrl() || '';\n  const m = url.match(/\\/s\\/([^/]+)\\/exec/i);\n  return m ? m[1] : '';\n}\n\nfunction compareVersions_(a,b) {\n  const aa=String(a||'0').split('.').map(function(x){return parseInt(x,10)||0;});\n  const bb=String(b||'0').split('.').map(function(x){return parseInt(x,10)||0;});\n  const n=Math.max(aa.length,bb.length);\n  for(let i=0;i<n;i++){\n    const x=aa[i]||0,y=bb[i]||0;\n    if(x>y)return 1;\n    if(x<y)return -1;\n  }\n  return 0;\n}\n\nfunction ensureSetting_(sheet, key, value) {\n  const n=Math.max(0,sheet.getLastRow()-1);\n  const vals=n ? sheet.getRange(2,1,n,2).getValues() : [];\n  const idx=vals.findIndex(function(r){return r[0]===key;});\n  if(idx<0) sheet.appendRow([key,value]);\n}\n\nfunction setSetting_(ss, key, value) {\n  const sheet=ss.getSheetByName(SHEETS.SETTINGS);\n  if(!sheet)throw new Error('Settingsシートが見つかりません。');\n  const n=Math.max(0,sheet.getLastRow()-1);\n  const vals=n ? sheet.getRange(2,1,n,2).getValues() : [];\n  const idx=vals.findIndex(function(r){return r[0]===key;});\n  if(idx>=0) sheet.getRange(idx+2,2).setValue(value);\n  else sheet.appendRow([key,value]);\n}\n\nfunction persistUpdateManifestUrl_(ss, url) {\n  const normalized = String(url || '').trim() || DEFAULT_UPDATE_MANIFEST_URL;\n  setSetting_(ss, '更新マニフェストURL', normalized);\n  PropertiesService.getScriptProperties().setProperty(UPDATE_MANIFEST_URL_PROPERTY, normalized);\n  return normalized;\n}\n\nfunction getUpdateManifestUrl_(ss) {\n  const settings = getSettings_(ss);\n  const sheetUrl = String(settings['更新マニフェストURL'] || '').trim();\n  const propertyUrl = String(PropertiesService.getScriptProperties().getProperty(UPDATE_MANIFEST_URL_PROPERTY) || '').trim();\n  return persistUpdateManifestUrl_(ss, sheetUrl || propertyUrl || DEFAULT_UPDATE_MANIFEST_URL);\n}\n\nfunction errorMessage_(e) {\n  return e && e.message ? e.message : String(e || '不明なエラー');\n}\n\nfunction assertAdmin_(pin) {\n  const settings = getSettings_(getSS_());\n  const expected = String(settings['本部管理PIN'] || '');\n  if (!expected || String(pin || '') !== expected) throw new Error('本部管理PINが違います。');\n}\n\nfunction getCachedJson_(key) {\n  try {\n    const text = CacheService.getScriptCache().get(key);\n    return text ? JSON.parse(text) : null;\n  } catch (e) {\n    return null;\n  }\n}\n\nfunction putCachedJson_(key, value, seconds) {\n  try {\n    CacheService.getScriptCache().put(key, JSON.stringify(value), seconds);\n  } catch (e) {\n    // キャッシュの容量超過時も、通常のシート読み込みを継続します。\n  }\n}\n\nfunction clearInventoryCache_(store) {\n  try {\n    CacheService.getScriptCache().remove(APP_CACHE_KEYS.INVENTORY_PREFIX + encodeURIComponent(String(store || '').trim()));\n  } catch (e) {}\n}\n\nfunction clearProductCache_() {\n  try {\n    const cache = CacheService.getScriptCache();\n    cache.remove(APP_CACHE_KEYS.PRODUCTS);\n    const settings = getSettings_(getSS_());\n    const stores = String(settings['店舗一覧'] || '').split(',').map(function(s){return s.trim();}).filter(Boolean);\n    for (var i = 0; i < stores.length; i++) cache.remove(APP_CACHE_KEYS.INVENTORY_PREFIX + encodeURIComponent(stores[i]));\n  } catch (e) {}\n}\n\nfunction getProducts_(ss, store) {\n  const normalizedStore = String(store || '').trim();\n  return getAllProducts_(ss)\n    .filter(function(p){return p.active;})\n    .filter(function(p){return !normalizedStore || p.excludedStores.indexOf(normalizedStore) < 0;})\n    .sort(function(a,b){return a.displayOrder-b.displayOrder;});\n}\n\n\nfunction getSS_() {\n  const id = PropertiesService.getScriptProperties().getProperty('SPREADSHEET_ID');\n  if (id) return SpreadsheetApp.openById(id);\n  const active = SpreadsheetApp.getActiveSpreadsheet();\n  if (active) return active;\n  throw new Error('管理用スプレッドシートが登録されていません。Apps Scriptで upgradeToV5_2() を1回実行してください。');\n}\n\nfunction getSettings_(ss) {\n  const sheet = ss.getSheetByName(SHEETS.SETTINGS);\n  const out = {};\n  if (!sheet || sheet.getLastRow() <= 1) return out;\n  sheet.getRange(2,1,sheet.getLastRow()-1,2).getValues().forEach(r => out[r[0]] = r[1]);\n  return out;\n}\nfunction getOrCreateSheet_(ss,name){return ss.getSheetByName(name)||ss.insertSheet(name);}\n"
    },
    {
      "name": "Index",
      "type": "HTML",
      "source": "<!DOCTYPE html>\n<html>\n<head>\n<base target=\"_top\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\n<style>\n*{box-sizing:border-box}body{font-family:Arial,'Noto Sans JP',sans-serif;margin:0;background:#f5f6f8;color:#222}header{background:#111;color:#fff;padding:16px 18px;font-size:20px;font-weight:700;position:sticky;top:0;z-index:10}main{max-width:980px;margin:auto;padding:16px}.card{background:#fff;border-radius:12px;padding:14px;margin-bottom:14px;box-shadow:0 1px 5px rgba(0,0,0,.08)}label{font-size:13px;color:#555;display:block;margin-bottom:5px}select,input,textarea,button{font-size:16px}select,input,textarea{width:100%;padding:10px;border:1px solid #ccc;border-radius:8px;background:#fff}.row{display:grid;grid-template-columns:1fr 1fr;gap:10px}.tabs{display:flex;gap:7px;margin-bottom:14px;overflow:auto}.tab{flex:1;min-width:120px;padding:12px;border:none;border-radius:9px;background:#ddd}.tab.active{background:#111;color:#fff}.category{border:1px solid #e6e6e6;border-radius:10px;margin-bottom:10px;overflow:hidden}.category-head{width:100%;border:0;background:#f7f7f7;padding:14px;display:flex;justify-content:space-between;align-items:center;font-weight:700;text-align:left}.category-body{display:none;padding:0 14px}.category.open .category-body{display:block}.product,.supplier-row{border-top:1px solid #eee;padding:13px 0}.product:first-child,.supplier-row:first-child{border-top:none}.hq-order{border:1px solid #dfe3e8;border-left:5px solid #d5a11e;border-radius:12px;padding:16px;margin:12px 0;background:#fafbfc;box-shadow:0 2px 8px rgba(24,39,58,.07)}.hq-order.status-arranged{border-left-color:#2f855a;background:#f7fcf8}.hq-order .pname{font-size:17px}.history-delivery{display:inline-block;margin-top:7px;padding:5px 9px;border-radius:7px;background:#eaf7ec;color:#17672a;font-size:12px;font-weight:700}.pname{font-weight:700}.meta{font-size:12px;color:#666;margin-top:4px}.stockrow{display:grid;grid-template-columns:minmax(170px,210px) minmax(190px,1fr) 110px;gap:10px;align-items:stretch;margin-top:10px}\n.stock-panel,.order-panel{border:1px solid #e2e5e9;border-radius:10px;padding:10px 11px;min-width:0}\n.stock-panel{background:#f4f5f6}\n.order-panel{background:#fff8e8;border-color:#eadfbd}\n.stock-panel label,.order-panel label{font-weight:700;margin-bottom:7px}\n.stock-panel label{color:#4b5563}\n.order-panel label{color:#7a5a00}\n.stock-panel .stepper input{background:#fff}\n.order-panel .stepper input{background:#fffdf7}\n.order-panel .stepper button{background:#fffaf0;border-color:#d7c793}\n.stock-panel .stepper button{background:#fff;border-color:#c8cdd3}\n.stock-panel:focus-within{border-color:#aeb5bd;box-shadow:0 0 0 2px rgba(0,0,0,.035)}\n.order-panel:focus-within{border-color:#c8aa50;box-shadow:0 0 0 2px rgba(191,151,37,.10)}\n.last-order{display:inline-block;margin-top:7px;padding:5px 8px;border-radius:7px;background:#eef3f7;color:#425466;font-size:12px}\n.history-item{border:1px solid #dfe3e8;border-left:5px solid #64748b;border-radius:12px;padding:15px;margin:12px 0;background:#fafbfc;box-shadow:0 2px 7px rgba(24,39,58,.06)}.history-item.status-cancelled{border-left-color:#94a3b8;background:#f1f5f9;opacity:.82}.history-item .pname{font-size:17px}.history-date{font-size:14px;font-weight:700;color:#334155;margin-top:9px}.history-details{font-size:12px;color:#667085;margin-top:5px;line-height:1.6}.history-actions{display:flex;justify-content:flex-end;margin-top:12px}.history-actions button{min-height:42px}.update-important{color:#c62828;font-weight:700}.update-apply{background:#c62828!important}.update-apply:hover{background:#9d1f1f!important}.tab.has-pending{background:#fff1f1;color:#b42318;box-shadow:inset 0 0 0 2px #e5484d;font-weight:700}.tab.active.has-pending{background:#b42318;color:#fff}.pending-count{display:inline-flex;align-items:center;justify-content:center;min-width:24px;height:24px;padding:0 7px;margin-left:4px;border-radius:999px;background:#b42318;color:#fff;font-size:13px;font-weight:700}.tab.active.has-pending .pending-count{background:#fff;color:#b42318}.cart-pending-banner{margin-bottom:12px;padding:11px 13px;border:1px solid #ef9a9a;border-left:5px solid #c62828;border-radius:9px;background:#fff3f3;color:#a61b1b;font-weight:700}.confirm-store-label{text-align:center;color:#667085;font-size:13px;margin-top:8px}.confirm-store{text-align:center;color:#c62828;font-size:26px;font-weight:800;margin:6px 0 12px}.confirm-warning{text-align:center;line-height:1.6}.confirm-actions{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:18px}.store-required{padding:18px;border:2px dashed #e0a12b;border-radius:10px;background:#fff9e8;color:#8a5a00;text-align:center;font-weight:700}\n.delivery-range{display:grid;grid-template-columns:1fr auto 1fr;gap:7px;align-items:center;margin-top:10px;max-width:430px}.delivery-range input{min-width:0}.delivery-label{font-size:13px;font-weight:700;margin-top:10px}.delivery-saved{font-size:13px;color:#17672a;margin-top:7px;font-weight:700}\n.history-loading{display:flex;align-items:center;justify-content:center;gap:10px;min-height:92px;color:#666;font-size:14px}.history-spinner{width:20px;height:20px;border:3px solid #d9dfe5;border-top-color:#425466;border-radius:50%;animation:spin .75s linear infinite}\n.add-order-btn{align-self:stretch;min-height:66px;font-weight:700}.stepper{display:grid;grid-template-columns:44px minmax(58px,1fr) 44px;gap:5px;align-items:center}.stepper button{height:44px;border:1px solid #aaa;background:#fff;border-radius:8px;font-size:22px;font-weight:700}.stepper input{text-align:center;height:44px;padding:7px;cursor:pointer}.wheel-modal{display:none;position:fixed;inset:0;background:rgba(0,0,0,.48);z-index:1200;align-items:flex-end;justify-content:center}.wheel-modal.open{display:flex}.wheel-sheet{background:#fff;width:100%;max-width:520px;border-radius:18px 18px 0 0;padding:14px 16px 18px}.wheel-title{font-weight:700;font-size:17px;text-align:center;margin:2px 0 10px}.wheel-wrap{position:relative;height:252px;overflow:hidden}.wheel-list{height:252px;overflow-y:auto;scroll-snap-type:y mandatory;-webkit-overflow-scrolling:touch;padding:104px 0;scrollbar-width:none}.wheel-list::-webkit-scrollbar{display:none}.wheel-item{height:44px;display:flex;align-items:center;justify-content:center;scroll-snap-align:center;font-size:22px;color:#777}.wheel-item.selected{font-size:28px;font-weight:700;color:#111}.wheel-highlight{position:absolute;left:0;right:0;top:104px;height:44px;border-top:1px solid #ddd;border-bottom:1px solid #ddd;pointer-events:none}.wheel-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px}.wheel-actions button{height:46px;border-radius:10px;font-size:16px}.wheel-cancel{border:1px solid #aaa;background:#fff}.wheel-ok{border:0;background:#111;color:#fff;font-weight:700}.badge{display:inline-block;padding:4px 7px;border-radius:999px;font-size:12px;background:#e8f4ea;color:#146c2e}.badge.low{background:#fdeaea;color:#a21d1d}.save-state{font-size:12px;margin-top:5px;min-height:16px;color:#777}.saved{color:#17672a}.failed{color:#9e1c1c}button.primary{width:100%;border:none;border-radius:10px;padding:13px;background:#111;color:#fff;font-weight:700}.secondary{border:1px solid #aaa;border-radius:8px;background:#fff;padding:9px}.linkbtn{display:inline-block;text-decoration:none;border-radius:8px;background:#111;color:#fff;padding:9px 12px;font-size:14px}.linkbtn.disabled{background:#bbb;pointer-events:none}.cart-item{display:flex;justify-content:space-between;gap:12px;padding:9px 0;border-bottom:1px solid #eee}.muted{color:#777;font-size:13px}.success{background:#eaf7ec;color:#17672a;padding:10px;border-radius:8px;margin-bottom:10px}.error{background:#fdeaea;color:#9e1c1c;padding:10px;border-radius:8px;margin-bottom:10px}.hq-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.hq-filters{display:grid;grid-template-columns:1fr 180px;gap:8px;margin-bottom:12px}.supplier-grid{display:grid;grid-template-columns:160px 1fr 1fr;gap:8px;align-items:end;margin-top:8px}.supplier-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}.memo{font-size:13px;background:#f7f7f7;border-radius:8px;padding:8px 10px;margin-top:8px;white-space:pre-wrap}.imagebtn{border:1px solid #aaa;border-radius:8px;background:#fff;padding:9px 12px}.product-image-btn{padding:4px 8px;font-size:12px;margin-left:6px;vertical-align:middle}.modal{display:none;position:fixed;inset:0;background:rgba(0,0,0,.62);z-index:1000;padding:18px;align-items:center;justify-content:center}.modal.open{display:flex}.modal-box{background:#fff;border-radius:14px;max-width:760px;width:100%;max-height:90vh;overflow:auto;padding:14px}.modal-head{display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:10px}.modal-close{border:0;background:#eee;border-radius:999px;width:38px;height:38px;font-size:22px}.product-image{display:block;max-width:100%;max-height:65vh;margin:0 auto;border-radius:10px;object-fit:contain}.image-error{padding:30px;text-align:center;color:#9e1c1c;background:#fdeaea;border-radius:10px}.pinbox{max-width:430px;margin:auto;padding:25px 10px}.section-title{display:flex;justify-content:space-between;align-items:center;gap:10px}.pill{font-size:12px;padding:4px 8px;border-radius:999px;background:#eee}.pill.pending{background:#fff0cf;color:#875c00}.pill.done{background:#eaf7ec;color:#17672a}\n\n\nbutton,.linkbtn{cursor:pointer;transition:background-color .16s ease,color .16s ease,border-color .16s ease,transform .08s ease,box-shadow .16s ease}\nbutton:disabled{cursor:not-allowed}\n@media (hover:hover) and (pointer:fine){\n  .tab:not(.active):hover{background:#c9c9c9}\n  .tab.active:hover,button.primary:hover,.wheel-ok:hover,.linkbtn:not(.disabled):hover{background:#333;color:#fff}\n  .secondary:hover,.imagebtn:hover,.wheel-cancel:hover,.stepper button:hover{background:#f0f0f0;border-color:#777}\n  .category-head:hover{background:#ececec}\n  .modal-close:hover{background:#d9d9d9}\n  .danger:hover{background:#c62828;color:#fff;border-color:#c62828}\n  button:not(:disabled):hover,.linkbtn:not(.disabled):hover{box-shadow:0 2px 8px rgba(0,0,0,.12)}\n}\nbutton:not(:disabled):active,.linkbtn:not(.disabled):active{transform:scale(.97);box-shadow:none}\n.stepper button:not(:disabled):active{transform:scale(.93)}\n.toast-host{position:fixed;top:18px;left:50%;transform:translateX(-50%);z-index:2500;width:min(92vw,560px);pointer-events:none}.toast{display:flex;align-items:center;gap:10px;padding:13px 16px;border-radius:12px;box-shadow:0 8px 28px rgba(0,0,0,.20);font-weight:700;opacity:0;transform:translateY(-10px);transition:opacity .18s ease,transform .18s ease;background:#fff;border:1px solid #ddd}.toast.show{opacity:1;transform:translateY(0)}.toast.success{background:#effaf1;color:#17672a;border-color:#b9dfc0}.toast.error{background:#fff1f1;color:#9e1c1c;border-color:#efbcbc}.toast.info{background:#f4f7fb;color:#29455f;border-color:#cbd9e7}.btn-busy{opacity:.72;cursor:wait!important}.btn-spinner{display:inline-block;width:15px;height:15px;border:2px solid currentColor;border-right-color:transparent;border-radius:50%;vertical-align:-2px;margin-right:7px;animation:spin .7s linear infinite}@keyframes spin{to{transform:rotate(360deg)}}\n\n.hq-subtabs{display:flex;gap:7px;margin-bottom:12px;overflow:auto;position:sticky;top:58px;z-index:8;background:#f5f6f8;padding:6px 0}\n.hq-subtab{flex:1;min-width:150px;padding:11px 12px;border:1px solid #ccc;border-radius:9px;background:#fff;font-weight:700;white-space:nowrap}\n.hq-subtab.active{background:#111;color:#fff;border-color:#111}\n.hq-panel{display:none}.hq-panel.active{display:block}\n@media (hover:hover) and (pointer:fine){.hq-subtab:not(.active):hover{background:#ececec;border-color:#999}.hq-subtab.active:hover{background:#333}}\n@media(max-width:650px){.hq-subtabs{top:54px}.hq-subtab{min-width:135px;font-size:14px;padding:10px}}\n@media(max-width:650px){.row{grid-template-columns:1fr}.stockrow{grid-template-columns:1fr 1fr;align-items:stretch;gap:8px}.stockrow>.secondary{grid-column:1/-1;width:100%;height:46px}.stock-panel,.order-panel{padding:9px}.supplier-grid,.hq-filters{grid-template-columns:1fr}.tabs{gap:5px}.tab{font-size:14px;padding:11px 8px}.stepper{grid-template-columns:42px minmax(52px,1fr) 42px}.stepper button,.stepper input{height:46px}}\n@media(max-width:410px){.stockrow{grid-template-columns:1fr}.stockrow>.secondary{grid-column:auto}.stock-panel,.order-panel{padding:10px 11px}}\n\n.admin-add-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.master-row{border:1px solid #dfe3e8;border-radius:12px;padding:13px;margin-top:12px;background:#fafbfc;box-shadow:0 2px 7px rgba(24,39,58,.06)}.master-row.drag-over{border-color:#2f6fed;box-shadow:0 0 0 3px rgba(47,111,237,.18)}.master-grid{display:grid;grid-template-columns:1.1fr 1.8fr .75fr .7fr .9fr .9fr 1fr;gap:7px;align-items:end}.master-supplier-section{margin-top:12px;padding-top:11px;border-top:1px solid #dfe3e8}.master-section-title{font-size:13px;font-weight:700;color:#425466;margin-bottom:6px}.master-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.master-heading{display:flex;align-items:center;justify-content:space-between;gap:12px}.master-heading-main{display:flex;align-items:center;gap:7px;flex-wrap:wrap}.master-status{display:inline-block;padding:3px 7px;border-radius:999px;background:#eaf7ec;color:#17672a;font-size:11px;font-weight:700}.master-status.missing{background:#fff0cf;color:#875c00}.master-toggle{flex:none;padding:6px 10px;font-size:12px}.master-drag-handle{flex:none;width:30px;height:30px;padding:0;border:1px solid #b7c2cf;border-radius:7px;background:#fff;color:#516173;font-size:17px;cursor:grab;line-height:1}.master-drag-handle:active{cursor:grabbing}.master-toolbar{display:flex;gap:7px;align-items:center;flex-wrap:wrap}.master-order-save[disabled]{opacity:.45;cursor:not-allowed}.master-row.master-collapsed>:not(.master-heading){display:none!important}.master-row.master-collapsed{padding-top:10px;padding-bottom:10px}.inactive{opacity:.55}.danger{background:#fff;border:1px solid #c62828;color:#c62828}\n.inventory-toolbar{display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:12px}.inventory-toolbar .muted{margin:0}.print-sheet{display:none}.print-sheet h1{font-size:22px;margin:0 0 12px}.print-meta{display:grid;grid-template-columns:1fr 1fr 1fr;gap:12px;margin-bottom:14px}.print-meta-item{border-bottom:1px solid #222;padding:4px 2px;min-height:28px}.print-table{width:100%;border-collapse:collapse;font-size:11px}.print-table th,.print-table td{border:1px solid #333;padding:5px 6px;vertical-align:middle}.print-table th{background:#e9ecef}.print-category td{background:#dfe3e8;font-weight:700;font-size:12px}.print-check{width:28px;text-align:center}.print-box{display:inline-block;width:14px;height:14px;border:1.5px solid #111}.print-number{width:74px;height:24px}.print-note{font-size:10px;margin-top:10px}.print-button{white-space:nowrap}\n.staff-select-row{display:flex;gap:8px;align-items:center}.staff-select-row select{flex:1;min-width:0}.staff-edit{white-space:nowrap;min-height:44px}.staff-manager{margin-top:8px;padding:9px;border:1px solid #dfe3e8;border-radius:9px;background:#fafbfc}.staff-manager-title{font-size:12px;color:#52606d;margin-bottom:6px}.staff-manager-list{display:flex;flex-wrap:wrap;gap:6px}.staff-manager-item{display:flex;align-items:center;gap:5px;padding:5px 6px 5px 8px;border:1px solid #ccd5df;border-radius:999px;background:#fff;font-size:13px}.staff-delete{width:25px;height:25px;border:0;border-radius:999px;background:#fbe9e9;color:#a51d1d;font-size:16px;line-height:1;cursor:pointer}\n.store-exclusions{margin-top:12px;padding:10px;border:1px solid #e1c86f;border-radius:10px;background:#fff9e8}.store-exclusion-options{display:flex;gap:6px;flex-wrap:wrap;margin-top:6px}.store-exclusion-option{display:flex;align-items:center;gap:5px;margin:0;padding:6px 8px;border:1px solid #d8c580;border-radius:8px;background:#fff;color:#5f4b00;font-size:12px}.store-exclusion-option input{width:16px;height:16px;margin:0}.store-exclusion-note{font-size:11px;color:#765f13;margin-top:6px}\n.arrangement-box{margin-top:10px;padding:10px;border:1px solid #c8d4e2;border-radius:9px;background:#f5f8fc}.arrangement-title{font-size:13px;font-weight:700;color:#425466;margin-bottom:6px}.arrangement-options{display:flex;gap:7px;flex-wrap:wrap}.arrangement-option{display:flex;align-items:center;gap:5px;margin:0;padding:6px 8px;border:1px solid #b7c7d9;border-radius:7px;background:#fff;font-size:12px;color:#34495e}.arrangement-option input{width:16px;height:16px;margin:0}.arrangement-detail{margin-top:7px;font-size:13px;padding:7px 8px}.arrangement-saved{margin-top:8px;padding:7px 9px;border-radius:7px;background:#edf3fb;color:#365b7c;font-size:12px;font-weight:700;white-space:pre-wrap}\n@media(min-width:861px){body.hq-mode main{max-width:1440px}.hq-mode #hqOrders{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.hq-mode .hq-order{margin:0;display:flex;flex-direction:column;min-width:0}.hq-mode .hq-order .hq-actions{margin-top:auto;padding-top:10px}.hq-mode .master-row{display:grid;grid-template-columns:minmax(0,1.15fr) minmax(390px,.85fr);gap:8px 14px}.hq-mode .master-row>.pname{grid-column:1/-1;font-size:16px;padding-bottom:5px;border-bottom:1px solid #e2e6ea}.hq-mode .master-row>.master-grid{grid-column:1;grid-row:2/span 2;grid-template-columns:repeat(3,minmax(0,1fr));align-content:start}.hq-mode .master-row>.muted{grid-column:1;margin-top:0!important}.hq-mode .master-row>.store-exclusions{grid-column:2;grid-row:2;margin:0}.hq-mode .master-row>.master-supplier-section{grid-column:2;grid-row:3/span 2;margin:0;padding:10px;border:1px solid #dfe3e8;border-radius:10px;background:#fff}.hq-mode .master-row>.master-actions{grid-column:1/-1;justify-content:flex-end;margin-top:0;padding-top:8px;border-top:1px solid #e2e6ea}.hq-mode .master-row input,.hq-mode .master-row textarea{font-size:14px;padding:7px 8px}.hq-mode .master-row label{font-size:12px;margin-bottom:3px}.hq-mode .master-row .supplier-grid{grid-template-columns:1fr 1fr;gap:7px;margin-top:5px}.hq-mode .master-row .supplier-grid>div:nth-child(3){grid-column:1/-1}.hq-mode .master-row .supplier-actions{margin-top:6px}.hq-mode .master-row textarea{min-height:52px;resize:vertical}}\n@media(max-width:860px){.admin-add-grid,.master-grid{grid-template-columns:1fr 1fr}.hq-mode #hqOrders{display:block}}\n@page{size:A4 portrait;margin:10mm}\n@media print{body{background:#fff;color:#000}body>*:not(#inventoryPrintSheet){display:none!important}#inventoryPrintSheet{display:block!important}.print-sheet{font-family:Arial,'Noto Sans JP',sans-serif}.print-table tr{break-inside:avoid}.print-category{break-after:avoid}}\n</style>\n</head>\n<body>\n<div data-app-version=\"<?= appVersion ?>\" style=\"display:none\" aria-hidden=\"true\"></div>\n<header id=\"title\">消耗品管理・発注</header>\n<main>\n<div id=\"msg\" class=\"toast-host\" aria-live=\"polite\"></div>\n<div class=\"card row\"><div><label>店舗</label><select id=\"store\" onchange=\"onStoreChange()\"></select></div><div><label>担当者名</label><div class=\"staff-select-row\"><select id=\"user\" onchange=\"onStaffChange()\"></select><button type=\"button\" class=\"secondary staff-edit\" onclick=\"toggleStaffManager()\">担当者を編集</button></div><div id=\"staffManager\" class=\"staff-manager\" style=\"display:none\"></div></div></div>\n<div class=\"tabs\"><button class=\"tab active\" onclick=\"showTab('inventory',this)\">在庫・発注</button><button id=\"cartTabButton\" class=\"tab\" onclick=\"showTab('cart',this)\">発注リスト <span id=\"cartCount\" class=\"pending-count\" style=\"display:none\">0</span></button><button class=\"tab\" onclick=\"showTab('history',this);loadHistory()\">履歴</button><button class=\"tab\" onclick=\"showTab('system',this);loadSystemUpdateInfo(null)\">システム更新</button><button class=\"tab\" onclick=\"showTab('hq',this);openHQ()\">本部発注</button></div>\n<section id=\"inventoryTab\" class=\"card\"><div class=\"inventory-toolbar\"><div class=\"muted\">現在庫は入力後、自動保存されます。</div><button class=\"secondary print-button\" onclick=\"printInventoryChecklist(this)\">在庫チェックリストを印刷</button></div><div id=\"products\">読込中...</div></section>\n<section id=\"cartTab\" class=\"card\" style=\"display:none\"><h3>発注内容</h3><div id=\"cartPendingBanner\" class=\"cart-pending-banner\" style=\"display:none\"></div><div id=\"cart\"></div><label style=\"margin-top:12px\">備考</label><textarea id=\"note\" rows=\"3\"></textarea><button class=\"primary\" style=\"margin-top:12px\" onclick=\"submitOrder(this)\">本部へ発注依頼を送る</button></section>\n<section id=\"historyTab\" class=\"card\" style=\"display:none\"><h3>発注履歴</h3><div id=\"history\" aria-live=\"polite\"></div></section>\n<section id=\"systemTab\" class=\"card\" style=\"display:none\">\n  <div class=\"section-title\"><h3>システム更新</h3><button class=\"secondary\" onclick=\"loadSystemUpdateInfo(this)\">情報を更新</button></div>\n  <div class=\"muted\">正式なGitHub更新元から、安全に最新版へ更新します。</div>\n  <div style=\"margin-top:14px\"><label>現在のバージョン</label><div id=\"sysCurrentVersion\" class=\"pname\">バージョンを準備中…</div></div>\n  <div class=\"supplier-actions\"><button class=\"primary\" onclick=\"checkSystemUpdate(this)\">最新版を確認</button></div>\n  <div id=\"updateResult\" class=\"memo\" style=\"display:none;margin-top:12px\"></div>\n  <button id=\"applyUpdateBtn\" class=\"primary update-apply\" style=\"display:none;margin-top:12px\" onclick=\"applySystemUpdate(this)\">最新版に更新</button>\n  <div class=\"muted\" style=\"margin-top:14px\">更新前のコードはGoogle Driveへ自動バックアップされます。</div>\n</section>\n<section id=\"hqTab\" style=\"display:none\">\n<div id=\"hqLogin\" class=\"card pinbox\"><h3>本部管理</h3><div class=\"muted\" style=\"margin-bottom:12px\">本部管理PINを入力してください。</div><input id=\"adminPin\" type=\"password\" inputmode=\"numeric\" placeholder=\"PIN\"><button class=\"primary\" style=\"margin-top:10px\" onclick=\"loginHQ()\">開く</button></div>\n<div id=\"hqContent\" style=\"display:none\">\n  <div class=\"hq-subtabs\">\n    <button class=\"hq-subtab active\" onclick=\"showHQSubTab('orders',this)\">発注処理</button>\n    <button class=\"hq-subtab\" onclick=\"showHQSubTab('products',this)\">商品マスター</button>\n  </div>\n  <div id=\"hqOrdersPanel\" class=\"hq-panel active\">\n    <div class=\"card\"><div class=\"section-title\"><h3>本部発注一覧</h3><button class=\"secondary\" onclick=\"loadHQOrders()\">更新</button></div><div class=\"hq-filters\"><select id=\"hqStatus\" onchange=\"renderHQOrders()\"><option value=\"発注依頼\">発注依頼のみ</option><option value=\"すべて\">すべて</option><option value=\"手配済み\">手配済み</option></select><input id=\"hqSearch\" placeholder=\"商品名・店舗で検索\" oninput=\"renderHQOrders()\"></div><div id=\"hqOrders\"></div></div>\n  </div>\n  <div id=\"hqProductsPanel\" class=\"hq-panel\">\n    <div class=\"card\"><div class=\"section-title\"><h3>商品マスター管理</h3><div class=\"master-toolbar\"><button class=\"secondary\" onclick=\"setAllMasterDetails(true)\">すべて開く</button><button class=\"secondary\" onclick=\"setAllMasterDetails(false)\">すべて閉じる</button><button class=\"secondary\" onclick=\"loadProductAdminMaster()\">更新</button></div></div><div class=\"muted\">商品情報、仕入先、発注ページ、画像、メモを商品ごとにまとめて管理できます。発注ページと画像が登録済みの商品は自動的に閉じて表示します。</div><div class=\"admin-add-grid\" style=\"margin-top:12px\"><div><label>カテゴリ</label><input id=\"newCategory\" placeholder=\"例：梱包用品\"></div><div><label>商品名</label><input id=\"newProductName\" placeholder=\"例：養生テープ\"></div><div><label>在庫単位</label><input id=\"newUnit\" placeholder=\"例：巻\"></div><div><label>最低在庫</label><input id=\"newMinStock\" type=\"number\" min=\"0\" value=\"1\"></div><div><label>推奨発注数（購入単位）</label><input id=\"newSuggestedQty\" type=\"number\" min=\"1\" value=\"1\"></div><div><label>1発注あたり数量</label><input id=\"newPackQty\" type=\"number\" min=\"1\" value=\"1\"></div><div><label>発注単位名</label><input id=\"newOrderUnitName\" placeholder=\"例：セット\"></div></div><div class=\"muted\" style=\"margin-top:8px\">例：レジロールは「在庫単位＝巻」「1発注あたり数量＝10」「発注単位名＝セット」にすると、発注数1＝10巻として扱います。</div><button class=\"primary\" style=\"margin-top:10px\" onclick=\"addNewProduct(this)\">商品を追加</button><div id=\"productAdminMaster\" style=\"margin-top:16px\"></div></div>\n  </div>\n</div>\n</section>\n</main>\n<div id=\"inventoryPrintSheet\" class=\"print-sheet\" aria-hidden=\"true\"></div>\n<div id=\"numberPickerModal\" class=\"wheel-modal\" onclick=\"if(event.target===this)closeNumberPicker(false)\"><div class=\"wheel-sheet\"><div id=\"wheelTitle\" class=\"wheel-title\">数量を選択</div><div class=\"wheel-wrap\"><div class=\"wheel-highlight\"></div><div id=\"wheelList\" class=\"wheel-list\" onscroll=\"wheelScrolled()\"></div></div><div class=\"wheel-actions\"><button type=\"button\" class=\"wheel-cancel\" onclick=\"closeNumberPicker(false)\">キャンセル</button><button type=\"button\" class=\"wheel-ok\" onclick=\"closeNumberPicker(true)\">決定</button></div></div></div>\n<div id=\"imageModal\" class=\"modal\" onclick=\"if(event.target===this)closeImageModal()\"><div class=\"modal-box\"><div class=\"modal-head\"><div><div id=\"modalProductName\" class=\"pname\"></div><div id=\"modalProductMemo\" class=\"muted\"></div></div><button class=\"modal-close\" onclick=\"closeImageModal()\">×</button></div><div id=\"modalImageArea\"></div></div></div>\n<div id=\"orderConfirmModal\" class=\"modal\" onclick=\"if(event.target===this)closeOrderConfirm()\"><div class=\"modal-box\"><div class=\"modal-head\"><h3 style=\"margin:0\">発注店舗を確認</h3><button class=\"modal-close\" onclick=\"closeOrderConfirm()\">×</button></div><div class=\"confirm-store-label\">発注する店舗</div><div id=\"confirmOrderStore\" class=\"confirm-store\"></div><div id=\"confirmOrderMessage\" class=\"confirm-warning\"></div><div class=\"confirm-actions\"><button class=\"secondary\" onclick=\"closeOrderConfirm()\">戻って確認</button><button id=\"confirmOrderSendBtn\" class=\"primary\" onclick=\"confirmSubmitOrder()\">この店舗で送信</button></div></div></div>\n<script>\nvar products=[];\nvar adminProducts=[];\nvar availableStores=[];\nvar staffByStore={};\nvar cart={};\nvar cartStore='';\nvar orderSubmitting=false;\nvar pendingOrderSubmission=null;\nvar saveTimers={};\nvar openCategories={};\nvar adminPin='';\nvar hqOrders=[];\nvar historyCache={};\nvar historyLoading={};\nvar defaultManifestUrl='https://raw.githubusercontent.com/suguru1022-tech/consumables-order-app-updates/refs/heads/main/release-manifest.json';\nvar numberPicker={type:'',id:'',value:0,step:1,max:100,scrollTimer:null};\n\nwindow.onerror=function(message,source,lineno,colno,error){\n  var detail='画面スクリプトエラー: '+message+' (行 '+lineno+')';\n  var p=document.getElementById('products');\n  if(p){p.innerHTML='<div class=\"error\">'+esc(detail)+'</div>';}\n  showMsg(detail,'error');\n  return false;\n};\n\nfunction byId(id){return document.getElementById(id);}\nfunction errorText(e){if(!e)return '不明なエラー'; return e.message?e.message:String(e);}\nfunction init(){\n  var embeddedVersion=getEmbeddedAppVersion();\n  if(embeddedVersion)byId('sysCurrentVersion').textContent='v'+embeddedVersion;\n  var masterHost=byId('productAdminMaster');\n  if(masterHost&&!masterHost.getAttribute('data-observing')){masterHost.setAttribute('data-observing','true');new MutationObserver(function(){decorateProductMasterCards();enableProductMasterDragAndDrop();}).observe(masterHost,{childList:true});}\n  var ordersHost=byId('hqOrders');\n  if(ordersHost&&!ordersHost.getAttribute('data-observing')){ordersHost.setAttribute('data-observing','true');new MutationObserver(function(){decorateHQArrangementFields();decorateSavedArrangements();}).observe(ordersHost,{childList:true});}\n  byId('products').innerHTML='初期データを読み込んでいます...';\n  try{\n    google.script.run\n      .withSuccessHandler(function(data){\n        try{\n          data=data||{};\n          byId('title').textContent=data.appName||'消耗品管理・発注';\n          var stores=data.stores||[];\n          availableStores=stores.slice();\n          staffByStore=data.staffByStore||{};\n          var html='<option value=\"\">店舗を選択してください</option>';\n          for(var i=0;i<stores.length;i++){html+='<option value=\"'+escAttr(stores[i])+'\">'+esc(stores[i])+'</option>';}\n          byId('store').innerHTML=html;\n          renderStaffOptions('');\n          products=data.products||[];\n          if(!stores.length){\n            byId('products').innerHTML='<div class=\"error\">店舗一覧を読み込めませんでした。Settingsシートの「店舗一覧」を確認してください。</div>';\n            return;\n          }\n          byId('products').innerHTML='<div class=\"store-required\">最初に店舗を選択してください。<br>店舗を選択するまで在庫変更・発注はできません。</div>';\n        }catch(ex){\n          byId('products').innerHTML='<div class=\"error\">初期表示処理でエラー: '+esc(errorText(ex))+'</div>';\n        }\n      })\n      .withFailureHandler(function(e){\n        var t='初期データの読み込みに失敗しました: '+errorText(e);\n        byId('products').innerHTML='<div class=\"error\">'+esc(t)+'</div>';\n        showMsg(t,'error');\n      })\n      .getInitialData();\n  }catch(ex){\n    byId('products').innerHTML='<div class=\"error\">Google Apps Scriptの呼び出し開始に失敗しました: '+esc(errorText(ex))+'</div>';\n  }\n}\nfunction onStoreChange(){var selected=byId('store').value;if(cartStore&&selected!==cartStore&&!objectIsEmpty(cart)){var nextName=selected||'未選択';if(!window.confirm('発注リストには「'+cartStore+'」の未送信商品があります。\\n店舗を「'+nextName+'」へ変更すると、発注リストを削除します。続行しますか？')){byId('store').value=cartStore;return;}cart={};cartStore='';byId('note').value='';renderCart();showMsg('店舗変更のため、未送信の発注リストを削除しました。','info');}renderStaffOptions(selected);for(var k in saveTimers){if(saveTimers.hasOwnProperty(k))clearTimeout(saveTimers[k]);}if(!selected){products=[];byId('products').innerHTML='<div class=\"store-required\">最初に店舗を選択してください。<br>店舗を選択するまで在庫変更・発注はできません。</div>';return;}openCategories={};loadInventory();}\nfunction renderStaffOptions(store,selectedName){var select=byId('user'),manager=byId('staffManager');if(!select)return;var names=store&&staffByStore[store]?staffByStore[store]:[];var html='<option value=\"\">担当者を選択してください</option>';for(var i=0;i<names.length;i++)html+='<option value=\"'+escAttr(names[i])+'\"'+(names[i]===selectedName?' selected':'')+'>'+esc(names[i])+'</option>';html+='<option value=\"__new__\">＋ 新しい担当者を登録</option>';select.innerHTML=html;select.disabled=!store;if(manager)manager.style.display='none';}\nfunction onStaffChange(){var select=byId('user');if(!select||select.value!=='__new__')return;var store=byId('store').value;if(!store){showMsg('先に店舗を選択してください。','error');renderStaffOptions('');return;}var name=window.prompt('新しい担当者名を入力してください。');if(name===null){renderStaffOptions(store);return;}name=String(name).replace(/^\\s+|\\s+$/g,'');if(!name){showMsg('担当者名を入力してください。','error');renderStaffOptions(store);return;}google.script.run.withSuccessHandler(function(r){staffByStore[store]=r.names||[];renderStaffOptions(store,name);showMsg('担当者を登録しました。','success');}).withFailureHandler(function(e){renderStaffOptions(store);showMsg(errorText(e),'error');}).saveStaffName({store:store,name:name});}\nfunction toggleStaffManager(){var store=byId('store').value,box=byId('staffManager');if(!store){showMsg('先に店舗を選択してください。','error');return;}if(box.style.display!=='none'){box.style.display='none';return;}renderStaffManager();box.style.display='block';}\nfunction renderStaffManager(){var store=byId('store').value,box=byId('staffManager'),names=staffByStore[store]||[];var html='<div class=\"staff-manager-title\">この店舗で選べる担当者名</div><div class=\"staff-manager-list\">';for(var i=0;i<names.length;i++)html+='<span class=\"staff-manager-item\">'+esc(names[i])+'<button type=\"button\" class=\"staff-delete\" aria-label=\"'+escAttr(names[i])+'を削除\" onclick=\"deleteStaffName(&quot;'+escAttr(names[i])+'&quot;)\">×</button></span>';html+='</div>';box.innerHTML=html+(names.length?'':'<div class=\"muted\">登録済みの担当者はいません。</div>');}\nfunction deleteStaffName(name){var store=byId('store').value;if(!window.confirm('「'+name+'」を選択肢から削除しますか？\\n過去の発注履歴には影響しません。'))return;google.script.run.withSuccessHandler(function(r){staffByStore[store]=r.names||[];renderStaffOptions(store);renderStaffManager();byId('staffManager').style.display='block';showMsg('担当者を削除しました。','success');}).withFailureHandler(function(e){showMsg(errorText(e),'error');}).deleteStaffName({store:store,name:name});}\nfunction loadInventory(){\n  var storeEl=byId('store');\n  if(!storeEl.value){byId('products').innerHTML='<div class=\"store-required\">最初に店舗を選択してください。<br>店舗を選択するまで在庫変更・発注はできません。</div>';return;}\n  byId('products').innerHTML='在庫データを読み込んでいます...';\n  google.script.run.withSuccessHandler(function(items){products=items||[];renderProducts();}).withFailureHandler(function(e){var t='在庫データの読み込みに失敗しました: '+errorText(e);byId('products').innerHTML='<div class=\"error\">'+esc(t)+'</div>';showMsg(t,'error');}).getInventory(storeEl.value);\n}\nfunction renderProducts(){\n  var groups={};var order=[];var i,p,cat;\n  for(i=0;i<products.length;i++){p=products[i];cat=p.category||'その他';if(!groups[cat]){groups[cat]=[];order.push(cat);}groups[cat].push(p);}\n  if(order.length && objectIsEmpty(openCategories))openCategories[order[0]]=true;\n  var html='';\n  for(var c=0;c<order.length;c++){\n    cat=order[c];var items=groups[cat],low=0;\n    for(i=0;i<items.length;i++){if(Number(items[i].currentStock)<=Number(items[i].minStock))low++;}\n    html+='<div class=\"category '+(openCategories[cat]?'open':'')+'\" data-cat=\"'+escAttr(cat)+'\"><button class=\"category-head\" onclick=\"toggleCat(this)\"><span>'+esc(cat)+'</span><span>要確認 '+low+'/'+items.length+' ▼</span></button><div class=\"category-body\">';\n    for(i=0;i<items.length;i++){\n      p=items[i];var isLow=Number(p.currentStock)<=Number(p.minStock);\n      var lastOrderText='前回発注：なし';\n      if(p.lastOrderDate){var lastTotal=(Number(p.lastOrderQty)||0)*(Number(p.packQty)||1);var lastDetail=Number(p.packQty)>1?'（合計'+esc(lastTotal)+esc(p.unit)+'）':'';lastOrderText='前回発注：'+esc(p.lastOrderDate)+' ／ '+esc(p.lastOrderQty)+esc(p.lastOrderUnit||p.orderUnitName||p.unit)+lastDetail;}\n      html+='<div class=\"product\"><div class=\"pname\">'+esc(p.name)+' <span id=\"badge_'+escAttr(p.id)+'\" class=\"badge '+(isLow?'low':'')+'\">'+(isLow?'要確認':'在庫OK')+'</span>'+(p.imageUrl?' <button type=\"button\" class=\"imagebtn product-image-btn\" onclick=\"openProductImage(&quot;'+escAttr(p.id)+'&quot;)\">画像を見る</button>':'')+'</div><div class=\"meta\">最低在庫 '+esc(p.minStock)+esc(p.unit)+' ／ 推奨発注 '+esc(p.suggestedQty)+esc(p.orderUnitName||p.unit)+(Number(p.packQty)>1?' ／ 1'+esc(p.orderUnitName||p.unit)+'='+esc(p.packQty)+esc(p.unit):'')+'</div><div class=\"last-order\">'+lastOrderText+'</div><div class=\"stockrow\"><div class=\"stock-panel\"><label>現在庫</label><div class=\"stepper\"><button type=\"button\" onclick=\"changeNumber(&quot;stock&quot;,&quot;'+escAttr(p.id)+'&quot;,-1)\">−</button><input type=\"number\" min=\"0\" id=\"stock_'+escAttr(p.id)+'\" value=\"'+escAttr(p.currentStock)+'\" oninput=\"stockChanged(&quot;'+escAttr(p.id)+'&quot;)\" onclick=\"openNumberPicker(&quot;stock&quot;,&quot;'+escAttr(p.id)+'&quot;)\"><button type=\"button\" onclick=\"changeNumber(&quot;stock&quot;,&quot;'+escAttr(p.id)+'&quot;,1)\">＋</button></div></div><div class=\"order-panel\"><label>発注数</label><div class=\"stepper\"><button type=\"button\" onclick=\"changeNumber(&quot;qty&quot;,&quot;'+escAttr(p.id)+'&quot;,-1)\">−</button><input type=\"number\" min=\"0\" step=\"1\" id=\"qty_'+escAttr(p.id)+'\" value=\"'+(isLow?escAttr(p.suggestedQty):'0')+'\" onclick=\"openNumberPicker(&quot;qty&quot;,&quot;'+escAttr(p.id)+'&quot;)\"><button type=\"button\" onclick=\"changeNumber(&quot;qty&quot;,&quot;'+escAttr(p.id)+'&quot;,1)\">＋</button></div></div><button class=\"secondary add-order-btn\" onclick=\"addToCart(&quot;'+escAttr(p.id)+'&quot;)\">追加</button></div><div id=\"save_'+escAttr(p.id)+'\" class=\"save-state\"></div></div>';\n    }\n    html+='</div></div>';\n  }\n  byId('products').innerHTML=html||'<div class=\"muted\">商品が登録されていません。</div>';\n  if(isMobilePicker()){var nums=byId('products').querySelectorAll('.stepper input');for(var n=0;n<nums.length;n++){nums[n].readOnly=true;nums[n].setAttribute('inputmode','none');}}\n}\nfunction printInventoryChecklist(btn){\n  var store=byId('store').value;\n  if(!store){showMsg('印刷する店舗を選択してください。','error');byId('store').focus();return;}\n  if(!products.length){showMsg('商品一覧を読み込んでから印刷してください。','error');return;}\n  setButtonBusy(btn,true,'印刷準備中…');\n  var now=new Date();\n  var weekdays=['日','月','火','水','木','金','土'];\n  var date=now.getFullYear()+'/'+('0'+(now.getMonth()+1)).slice(-2)+'/'+('0'+now.getDate()).slice(-2)+'（'+weekdays[now.getDay()]+'）';\n  var html='<h1>消耗品 在庫チェックリスト</h1><div class=\"print-meta\"><div class=\"print-meta-item\"><strong>店舗：</strong>'+esc(store)+'</div><div class=\"print-meta-item\"><strong>実施日：</strong>'+esc(date)+'</div><div class=\"print-meta-item\"><strong>確認者：</strong></div></div><table class=\"print-table\"><thead><tr><th class=\"print-check\">確認</th><th>商品名</th><th>在庫単位</th><th>最低在庫</th><th>実在庫</th><th>要発注</th></tr></thead><tbody>';\n  var groups={},categoryOrder=[];\n  for(var i=0;i<products.length;i++){var category=products[i].category||'その他';if(!groups[category]){groups[category]=[];categoryOrder.push(category);}groups[category].push(products[i]);}\n  for(var c=0;c<categoryOrder.length;c++){\n    var categoryName=categoryOrder[c],items=groups[categoryName];\n    html+='<tr class=\"print-category\"><td colspan=\"6\">'+esc(categoryName)+'</td></tr>';\n    for(var j=0;j<items.length;j++){\n      var p=items[j];\n      html+='<tr><td class=\"print-check\"><span class=\"print-box\"></span></td><td>'+esc(p.name)+'</td><td>'+esc(p.unit||'個')+'</td><td>'+esc(p.minStock)+esc(p.unit||'個')+'</td><td class=\"print-number\"></td><td class=\"print-check\"><span class=\"print-box\"></span></td></tr>';\n    }\n  }\n  html+='</tbody></table><div class=\"print-note\">実在庫を数えて記入し、確認後にアプリの「現在庫」へ入力してください。最低在庫以下の商品は「要発注」にチェックしてください。</div>';\n  var sheet=byId('inventoryPrintSheet');\n  sheet.innerHTML=html;\n  sheet.setAttribute('aria-hidden','false');\n  setButtonBusy(btn,false);\n  window.setTimeout(function(){window.print();},100);\n}\nfunction objectIsEmpty(o){for(var k in o){if(o.hasOwnProperty(k))return false;}return true;}\nfunction toggleCat(btn){var box=btn.parentNode;var cat=box.getAttribute('data-cat');if(box.className.indexOf(' open')>=0){box.className=box.className.replace(' open','');delete openCategories[cat];}else{box.className+=' open';openCategories[cat]=true;}}\nfunction findProduct(id){for(var i=0;i<products.length;i++){if(products[i].id===id)return products[i];}for(var j=0;j<adminProducts.length;j++){if(adminProducts[j].id===id)return adminProducts[j];}return null;}\nfunction isMobilePicker(){return window.matchMedia&&window.matchMedia('(max-width: 760px), (pointer: coarse)').matches;}\nfunction changeNumber(type,id,direction){var p=findProduct(id);if(!p)return;var el=byId((type==='stock'?'stock_':'qty_')+id);if(!el)return;var step=1;var v=Math.max(0,(Number(el.value)||0)+direction*step);el.value=v;if(type==='stock')stockChanged(id);}\nfunction openNumberPicker(type,id){if(!isMobilePicker())return;var p=findProduct(id);if(!p)return;var el=byId((type==='stock'?'stock_':'qty_')+id);if(!el)return;try{el.blur();}catch(e){}var step=1;var current=Math.max(0,Number(el.value)||0);var suggested=Math.max(Number(p.suggestedQty)||0,Number(p.minStock)||0,current);var max=Math.max(30,Math.ceil((suggested*4+20)/step)*step);max=Math.min(500,max);numberPicker={type:type,id:id,value:current,step:step,max:max,scrollTimer:null};byId('wheelTitle').textContent=(type==='stock'?'現在庫':'発注数')+'を選択';renderWheel();byId('numberPickerModal').className='wheel-modal open';setTimeout(function(){scrollWheelToValue(current,false);},20);}\nfunction renderWheel(){var list=byId('wheelList'),html='',step=numberPicker.step,max=numberPicker.max;for(var v=0;v<=max;v+=step){html+='<div class=\"wheel-item'+(v===numberPicker.value?' selected':'')+'\" data-value=\"'+v+'\" onclick=\"selectWheelValue('+v+',true)\">'+v+'</div>';}list.innerHTML=html;}\nfunction scrollWheelToValue(v,smooth){var list=byId('wheelList'),items=list.getElementsByClassName('wheel-item'),target=null;for(var i=0;i<items.length;i++){if(Number(items[i].getAttribute('data-value'))===Number(v)){target=items[i];break;}}if(target){var top=target.offsetTop-(list.clientHeight-target.offsetHeight)/2;try{list.scrollTo({top:top,behavior:smooth?'smooth':'auto'});}catch(e){list.scrollTop=top;}}}\nfunction selectWheelValue(v,scroll){numberPicker.value=Number(v)||0;updateWheelSelected();if(scroll)scrollWheelToValue(numberPicker.value,true);}\nfunction updateWheelSelected(){var items=byId('wheelList').getElementsByClassName('wheel-item');for(var i=0;i<items.length;i++){items[i].className='wheel-item'+(Number(items[i].getAttribute('data-value'))===Number(numberPicker.value)?' selected':'');}}\nfunction wheelScrolled(){if(numberPicker.scrollTimer)clearTimeout(numberPicker.scrollTimer);numberPicker.scrollTimer=setTimeout(function(){var list=byId('wheelList'),items=list.getElementsByClassName('wheel-item'),center=list.scrollTop+list.clientHeight/2,best=null,bestDist=999999;for(var i=0;i<items.length;i++){var c=items[i].offsetTop+items[i].offsetHeight/2,d=Math.abs(c-center);if(d<bestDist){bestDist=d;best=items[i];}}if(best){numberPicker.value=Number(best.getAttribute('data-value'))||0;updateWheelSelected();}},90);}\nfunction closeNumberPicker(apply){var m=byId('numberPickerModal');if(apply&&numberPicker.id){var el=byId((numberPicker.type==='stock'?'stock_':'qty_')+numberPicker.id);if(el){el.value=numberPicker.value;if(numberPicker.type==='stock')stockChanged(numberPicker.id);}}m.className='wheel-modal';}\nfunction stockChanged(id){\n  var p=findProduct(id);if(!p)return;\n  var el=byId('stock_'+id),v=Math.max(0,Number(el.value)||0);p.currentStock=v;\n  var isLow=v<=Number(p.minStock),b=byId('badge_'+id);if(b){b.textContent=isLow?'要確認':'在庫OK';b.className='badge '+(isLow?'low':'');}\n  if(saveTimers[id])clearTimeout(saveTimers[id]);\n  var s=byId('save_'+id);if(s){s.textContent='保存待ち…';s.className='save-state';}\n  saveTimers[id]=setTimeout(function(){google.script.run.withSuccessHandler(function(){var se=byId('save_'+id);if(se){se.textContent='✓ 保存済み';se.className='save-state saved';}}).withFailureHandler(function(e){var se=byId('save_'+id);if(se){se.textContent='保存失敗';se.className='save-state failed';}showMsg(errorText(e),'error');}).saveInventoryItem({store:byId('store').value,user:byId('user').value.replace(/^\\s+|\\s+$/g,''),productId:p.id,productName:p.name,currentStock:v});},700);\n}\nfunction addToCart(id){var store=byId('store').value;if(!store){showMsg('最初に店舗を選択してください。','error');byId('store').focus();return;}var p=findProduct(id);if(!p)return;var q=Number(byId('qty_'+id).value)||0;if(q<=0){showMsg('発注数を入力してください。','error');return;}if(cartStore&&cartStore!==store){showMsg('発注リストの店舗と選択中の店舗が一致しません。店舗を確認してください。','error');return;}if(!cartStore)cartStore=store;cart[id]={productId:p.id,productName:p.name,orderQty:q,unit:p.orderUnitName||p.unit,orderUnitName:p.orderUnitName||p.unit,stockUnit:p.unit,packQty:Math.max(1,Number(p.packQty)||1)};renderCart();showMsg(p.name+'を「'+store+'」の発注リストへ追加しました。','success');}\nfunction renderCart(){var arr=[];for(var k in cart){if(cart.hasOwnProperty(k))arr.push(cart[k]);}var count=arr.length,countBadge=byId('cartCount'),tabButton=byId('cartTabButton'),banner=byId('cartPendingBanner');if(!count)cartStore='';countBadge.textContent=count;countBadge.style.display=count?'inline-flex':'none';tabButton.className=tabButton.className.replace(/\\s*has-pending/g,'')+(count?' has-pending':'');banner.style.display=count?'block':'none';banner.textContent=count?'「'+cartStore+'」として本部へ未送信の発注が'+count+'件あります。発注リストを確認して送信してください。':'';var html='';for(var i=0;i<arr.length;i++){var x=arr[i];var total=(Number(x.orderQty)||0)*(Number(x.packQty)||1);var detail=(Number(x.packQty)||1)>1?'（合計 '+esc(total)+esc(x.stockUnit)+'）':'';html+='<div class=\"cart-item\"><div>'+esc(x.productName)+'<div class=\"muted\">'+esc(x.orderQty)+esc(x.orderUnitName||x.unit)+' '+detail+'</div></div><button class=\"secondary\" onclick=\"removeCart(&quot;'+escAttr(x.productId)+'&quot;)\">削除</button></div>';}byId('cart').innerHTML=html||'<div class=\"muted\">発注商品はありません。</div>';}\nfunction removeCart(id){delete cart[id];renderCart();}\nfunction submitOrder(btn){if(orderSubmitting||pendingOrderSubmission)return;var store=byId('store').value,user=byId('user').value;if(!store){showMsg('店舗を選択してください。','error');byId('store').focus();return;}if(!user||user==='__new__'){showMsg('担当者を選択するか、新しい担当者を登録してから発注してください。','error');byId('user').focus();return;}if(!cartStore||cartStore!==store){showMsg('発注リストの店舗と選択中の店舗が一致しません。店舗を確認してください。','error');return;}var items=[];for(var k in cart){if(cart.hasOwnProperty(k))items.push(cart[k]);}if(!items.length){showMsg('発注商品がありません。','error');return;}pendingOrderSubmission={button:btn,store:store,user:String(user).replace(/^\\s+|\\s+$/g,''),note:byId('note').value.replace(/^\\s+|\\s+$/g,''),items:items};byId('confirmOrderStore').textContent=store;byId('confirmOrderMessage').textContent='「'+store+'」として'+items.length+'商品の発注依頼を本部へ送信します。店舗に間違いがないか確認してください。';byId('orderConfirmModal').className='modal open';}\nfunction closeOrderConfirm(){if(orderSubmitting)return;pendingOrderSubmission=null;byId('orderConfirmModal').className='modal';}\nfunction confirmSubmitOrder(){if(orderSubmitting||!pendingOrderSubmission)return;var payload=pendingOrderSubmission,mainBtn=payload.button,confirmBtn=byId('confirmOrderSendBtn');orderSubmitting=true;setButtonBusy(mainBtn,true,'送信中…');setButtonBusy(confirmBtn,true,'送信中…');google.script.run.withSuccessHandler(function(r){orderSubmitting=false;setButtonBusy(mainBtn,false);setButtonBusy(confirmBtn,false);pendingOrderSubmission=null;byId('orderConfirmModal').className='modal';cart={};cartStore='';renderCart();byId('note').value='';showMsg('発注依頼を送信しました。発注ID：'+r.orderId,'success');showTab('history',document.getElementsByClassName('tab')[2]);loadHistory(true);}).withFailureHandler(function(e){orderSubmitting=false;setButtonBusy(mainBtn,false);setButtonBusy(confirmBtn,false);showMsg(errorText(e),'error');}).submitOrder({store:payload.store,user:payload.user,note:payload.note,items:payload.items});}\nfunction renderHistory(rows){rows=rows||[];var html='';for(var i=0;i<rows.length;i++){var r=rows[i];var delivery=(r.deliveryFrom||r.deliveryTo)?'<div class=\"history-delivery\">納品予定：'+esc(r.deliveryFrom||r.deliveryTo)+(r.deliveryTo&&r.deliveryTo!==r.deliveryFrom?' ～ '+esc(r.deliveryTo):'')+'</div>':'';var cancel=r.status==='発注依頼'?'<div class=\"history-actions\"><button class=\"danger\" onclick=\"cancelHistoryOrder('+Number(r.rowNumber)+',&quot;'+escAttr(r.orderId)+'&quot;,&quot;'+escAttr(r.store)+'&quot;,this)\">発注を取り下げる</button></div>':'';html+='<div class=\"history-item '+(r.status==='キャンセル'?'status-cancelled':'')+'\"><div class=\"pname\">'+esc(r.productName)+' × '+esc(r.qty)+esc(r.unit)+'</div><div class=\"history-date\">発注日：'+esc(r.date||'日時不明')+'</div><div class=\"history-details\">発注番号：'+esc(r.orderId)+'<br>店舗：'+esc(r.store)+' ／ 担当者：'+esc(r.user||'未入力')+' ／ 状態：'+esc(r.status)+'</div>'+delivery+cancel+'</div>';}byId('history').innerHTML=html||'<div class=\"muted\">履歴はありません。</div>';}\nfunction cancelHistoryOrder(row,orderId,store,btn){if(!window.confirm('この商品の発注依頼を取り下げますか？\\n本部で手配済みの場合は取り下げできません。'))return;setButtonBusy(btn,true,'取り下げ中…');google.script.run.withSuccessHandler(function(){delete historyCache[store];showMsg('発注依頼を取り下げました。','success');loadHistory(true);if(adminPin)loadHQOrders();}).withFailureHandler(function(e){setButtonBusy(btn,false);showMsg(errorText(e),'error');loadHistory(true);}).cancelOrderLine({rowNumber:row,orderId:orderId,store:store});}\nfunction loadHistory(force){var store=byId('store').value;if(!store){byId('history').innerHTML='<div class=\"store-required\">履歴を表示する店舗を選択してください。</div>';return;}if(!force&&historyCache.hasOwnProperty(store)){renderHistory(historyCache[store]);return;}if(historyLoading[store]){byId('history').innerHTML='<div class=\"history-loading\" role=\"status\"><span class=\"history-spinner\" aria-hidden=\"true\"></span><span>発注履歴を読み込み中…</span></div>';return;}historyLoading[store]=true;byId('history').innerHTML='<div class=\"history-loading\" role=\"status\"><span class=\"history-spinner\" aria-hidden=\"true\"></span><span>発注履歴を読み込み中…</span></div>';google.script.run.withSuccessHandler(function(rows){delete historyLoading[store];historyCache[store]=rows||[];if(byId('store').value===store)renderHistory(historyCache[store]);}).withFailureHandler(function(e){delete historyLoading[store];if(byId('store').value!==store)return;var t='発注履歴の読み込みに失敗しました: '+errorText(e);byId('history').innerHTML='<div class=\"error\">'+esc(t)+'</div><button class=\"secondary\" onclick=\"loadHistory(true)\">再読み込み</button>';showMsg(t,'error');}).getOrderHistory(store);}\nfunction openHQ(){if(adminPin){byId('hqLogin').style.display='none';byId('hqContent').style.display='block';showHQSubTab('orders',null);}else{byId('hqLogin').style.display='block';byId('hqContent').style.display='none';}}\nfunction loginHQ(){var p=byId('adminPin').value.replace(/^\\s+|\\s+$/g,'');google.script.run.withSuccessHandler(function(){adminPin=p;byId('hqLogin').style.display='none';byId('hqContent').style.display='block';showHQSubTab('orders',null);}).withFailureHandler(function(e){showMsg(errorText(e),'error');}).verifyAdminPin(p);}\nfunction showHQSubTab(name,button){var map={orders:'hqOrdersPanel',products:'hqProductsPanel'};for(var k in map){if(map.hasOwnProperty(k)){var panel=byId(map[k]);if(panel)panel.className='hq-panel'+(k===name?' active':'');}}var tabs=document.getElementsByClassName('hq-subtab');for(var i=0;i<tabs.length;i++)tabs[i].className='hq-subtab';if(button)button.className='hq-subtab active';else{var idx=name==='orders'?0:1;if(tabs[idx])tabs[idx].className='hq-subtab active';}if(name==='orders')loadHQOrders();if(name==='products')loadProductAdminMaster();}\nfunction loadHQOrders(){google.script.run.withSuccessHandler(function(rows){hqOrders=rows||[];renderHQOrders();}).withFailureHandler(function(e){showMsg(errorText(e),'error');}).getHeadOfficeOrders(adminPin);}\nfunction renderHQOrders(){var f=byId('hqStatus').value,q=byId('hqSearch').value.toLowerCase().replace(/^\\s+|\\s+$/g,''),html='';for(var i=0;i<hqOrders.length;i++){var r=hqOrders[i];var displayStatus=r.status==='納品済み'?'手配済み':r.status;var statusOk=f==='すべて'||displayStatus===f;var text=(String(r.productName||'')+String(r.store||'')+String(r.orderId||'')).toLowerCase();if(!statusOk||(q&&text.indexOf(q)<0))continue;var packQty=Math.max(1,Number(r.packQty)||1);var purchaseDetail=packQty>1?'<div class=\"meta\">合計 '+esc((Number(r.qty)||0)*packQty)+esc(r.stockUnit)+' ／ 1'+esc(r.orderUnitName||r.unit)+'='+esc(packQty)+esc(r.stockUnit)+'</div>':'';var delivery=(r.deliveryFrom||r.deliveryTo)?'<div class=\"delivery-saved\">納品予定：'+esc(r.deliveryFrom||r.deliveryTo)+(r.deliveryTo&&r.deliveryTo!==r.deliveryFrom?' ～ '+esc(r.deliveryTo):'')+'</div>':'';var arrangeArea=displayStatus==='手配済み'?delivery:'<div class=\"delivery-label\">納品予定</div><div class=\"delivery-range\"><input id=\"deliveryFrom_'+Number(r.rowNumber)+'\" type=\"date\" aria-label=\"納品予定開始日\"><span>～</span><input id=\"deliveryTo_'+Number(r.rowNumber)+'\" type=\"date\" aria-label=\"納品予定終了日\"></div>';html+='<div class=\"hq-order '+(displayStatus==='手配済み'?'status-arranged':'status-requested')+'\"><div class=\"section-title\"><div><div class=\"pname\">'+esc(r.productName)+' × '+esc(r.qty)+esc(r.unit)+'</div>'+purchaseDetail+'<div class=\"meta\">'+esc(r.store)+' ／ '+esc(r.user||'担当者未入力')+' ／ '+esc(r.orderId)+'</div></div><span class=\"pill '+(displayStatus==='手配済み'?'done':'pending')+'\">'+esc(displayStatus)+'</span></div>'+(r.supplier?'<div class=\"meta\">仕入先：'+esc(r.supplier)+'</div>':'')+(r.productMemo?'<div class=\"memo\">'+esc(r.productMemo)+'</div>':'')+arrangeArea+'<div class=\"hq-actions\">'+(r.imageUrl?'<button class=\"imagebtn\" onclick=\"openProductImage(&quot;'+escAttr(r.productId)+'&quot;)\">画像を見る</button>':'<span class=\"linkbtn disabled\">画像未登録</span>')+(r.orderUrl?'<a class=\"linkbtn\" href=\"'+escAttr(r.orderUrl)+'\" target=\"_blank\" rel=\"noopener\">発注ページを開く</a>':'<span class=\"linkbtn disabled\">URL未登録</span>')+(displayStatus==='手配済み'?'':'<button class=\"secondary\" onclick=\"setLineStatus('+Number(r.rowNumber)+',&quot;手配済み&quot;,this)\">手配済みにする</button>')+'</div></div>';}\n  byId('hqOrders').innerHTML=html||'<div class=\"muted\">該当する発注はありません。</div>';\n  addDeliveryModeSelectors();\n}\nfunction addDeliveryModeSelectors(){var ranges=document.getElementsByClassName('delivery-range');for(var i=0;i<ranges.length;i++){var first=ranges[i].getElementsByTagName('input')[0];if(!first)continue;var row=String(first.id||'').replace('deliveryFrom_','');if(!row||byId('deliveryMode_'+row))continue;var select=document.createElement('select');select.id='deliveryMode_'+row;select.setAttribute('aria-label','納品予定の設定方法');select.style.margin='8px 0';select.innerHTML='<option value=\"date\">日付を指定</option><option value=\"undecided\">未確定</option>';select.setAttribute('onchange','toggleDeliveryMode('+Number(row)+')');ranges[i].parentNode.insertBefore(select,ranges[i]);}}\nfunction toggleDeliveryMode(row){var mode=byId('deliveryMode_'+row),range=byId('deliveryFrom_'+row);if(!mode||!range)return;var wrap=range.parentNode;var undecided=mode.value==='undecided';wrap.style.display=undecided?'none':'grid';if(undecided){byId('deliveryFrom_'+row).value='';byId('deliveryTo_'+row).value='';}}\nfunction setLineStatus(row,status,btn){var mode=byId('deliveryMode_'+row);var deliveryUndecided=!!(mode&&mode.value==='undecided');var deliveryFrom=byId('deliveryFrom_'+row)?byId('deliveryFrom_'+row).value:'';var deliveryTo=byId('deliveryTo_'+row)?byId('deliveryTo_'+row).value:'';if(status==='手配済み'&&!deliveryUndecided&&!deliveryFrom){showMsg('納品予定の開始日を入力するか「未確定」を選択してください。','error');return;}if(status==='手配済み'&&!deliveryUndecided&&!deliveryTo)deliveryTo=deliveryFrom;if(!deliveryUndecided&&deliveryTo&&deliveryFrom&&deliveryTo<deliveryFrom){showMsg('終了日は開始日以降にしてください。','error');return;}setButtonBusy(btn,true,'更新中…');google.script.run.withSuccessHandler(function(){setButtonBusy(btn,false);showMsg('手配済みに更新しました。','success');loadHQOrders();}).withFailureHandler(function(e){setButtonBusy(btn,false);showMsg(errorText(e),'error');}).updateOrderLineStatus({pin:adminPin,rowNumber:row,status:status,deliveryFrom:deliveryFrom,deliveryTo:deliveryTo,deliveryUndecided:deliveryUndecided});}\nfunction renderExcludedStoreOptions(p){if(!availableStores.length)return '<div class=\"store-exclusions\"><div class=\"master-section-title\">店舗別の対象外設定</div><div class=\"store-exclusion-note\">Settingsシートの店舗一覧を確認してください。</div></div>';var excluded=p.excludedStores||[],html='<div class=\"store-exclusions\"><div class=\"master-section-title\">店舗別の対象外設定</div><div class=\"store-exclusion-options\">';for(var i=0;i<availableStores.length;i++){var store=availableStores[i],id='mex_'+p.id+'_'+i;html+='<label class=\"store-exclusion-option\" for=\"'+escAttr(id)+'\"><input type=\"checkbox\" id=\"'+escAttr(id)+'\" data-store=\"'+escAttr(store)+'\"'+(excluded.indexOf(store)>=0?' checked':'')+'> '+esc(store)+'では対象外</label>';}return html+'</div><div class=\"store-exclusion-note\">対象外にした店舗では、在庫・発注画面と印刷チェックリストにこの商品を表示しません。</div></div>';}\nfunction loadProductAdminMaster(){google.script.run.withSuccessHandler(function(rows){var box=byId('productAdminMaster');var html='';adminProducts=rows||[];for(var i=0;i<adminProducts.length;i++){var p=adminProducts[i];html+='<div class=\"master-row '+(p.active?'':'inactive')+'\"><div class=\"pname\">'+esc(p.id)+'　'+esc(p.name)+(p.active?'':'　<span class=\"badge\">非表示</span>')+'</div><div class=\"master-grid\"><div><label>カテゴリ</label><input id=\"mcat_'+escAttr(p.id)+'\" value=\"'+escAttr(p.category||'')+'\"></div><div><label>商品名</label><input id=\"mname_'+escAttr(p.id)+'\" value=\"'+escAttr(p.name||'')+'\"></div><div><label>在庫単位</label><input id=\"munit_'+escAttr(p.id)+'\" value=\"'+escAttr(p.unit||'')+'\"></div><div><label>最低在庫</label><input id=\"mmin_'+escAttr(p.id)+'\" type=\"number\" min=\"0\" value=\"'+escAttr(p.minStock)+'\"></div><div><label>推奨発注数（購入単位）</label><input id=\"msug_'+escAttr(p.id)+'\" type=\"number\" min=\"1\" value=\"'+escAttr(p.suggestedQty)+'\"></div><div><label>1発注あたり数量</label><input id=\"mpack_'+escAttr(p.id)+'\" type=\"number\" min=\"1\" value=\"'+escAttr(p.packQty||1)+'\"></div><div><label>発注単位名</label><input id=\"mordername_'+escAttr(p.id)+'\" value=\"'+escAttr(p.orderUnitName||p.unit||'')+'\" placeholder=\"例：セット\"></div></div><div class=\"muted\" style=\"margin-top:6px\">1'+esc(p.orderUnitName||p.unit)+' = '+esc(p.packQty||1)+esc(p.unit)+'</div>'+renderExcludedStoreOptions(p)+'<div class=\"master-supplier-section\"><div class=\"master-section-title\">仕入先・画像</div><div class=\"supplier-grid\"><div><label>仕入先</label><input id=\"sup_'+escAttr(p.id)+'\" value=\"'+escAttr(p.supplier||'')+'\" placeholder=\"例：ASKUL\"></div><div><label>発注ページURL</label><input id=\"url_'+escAttr(p.id)+'\" value=\"'+escAttr(p.orderUrl||'')+'\" placeholder=\"https://...\"></div><div><label>商品画像URL</label><input id=\"img_'+escAttr(p.id)+'\" value=\"'+escAttr(p.imageUrl||'')+'\" placeholder=\"https://...\"></div></div><div style=\"margin-top:8px\"><label>商品メモ</label><textarea id=\"memo_'+escAttr(p.id)+'\" rows=\"2\" placeholder=\"例：通常レジロールと間違えない。型番○○\">'+esc(p.productMemo||'')+'</textarea></div><div style=\"margin-top:8px\"><label>商品画像をアップロード</label><input type=\"file\" id=\"file_'+escAttr(p.id)+'\" accept=\"image/*\"></div><div class=\"supplier-actions\"><button id=\"viewimg_'+escAttr(p.id)+'\" class=\"imagebtn\" style=\"'+(p.imageUrl?'':'display:none')+'\" onclick=\"openProductImage(&quot;'+escAttr(p.id)+'&quot;)\">登録画像を見る</button><button class=\"secondary\" onclick=\"uploadProductImageFile(&quot;'+escAttr(p.id)+'&quot;,this)\">画像アップロード</button></div></div><div class=\"master-actions\"><button class=\"secondary\" onclick=\"saveProductMaster(&quot;'+escAttr(p.id)+'&quot;,'+(p.active?'true':'false')+',this)\">商品情報を保存</button><button class=\"'+(p.active?'danger':'secondary')+'\" onclick=\"toggleProductActive(&quot;'+escAttr(p.id)+'&quot;,'+(p.active?'false':'true')+')\">'+(p.active?'非表示にする':'再表示する')+'</button></div></div>'; }box.innerHTML=html||'<div class=\"muted\">商品がありません。</div>';}).withFailureHandler(function(e){showMsg(errorText(e),'error');}).getProductAdminMaster(adminPin);}\nfunction addNewProduct(btn){setButtonBusy(btn,true,'追加中…');var payload={pin:adminPin,category:byId('newCategory').value,name:byId('newProductName').value,unit:byId('newUnit').value,minStock:byId('newMinStock').value,suggestedQty:byId('newSuggestedQty').value,packQty:byId('newPackQty').value,orderUnitName:byId('newOrderUnitName').value};google.script.run.withSuccessHandler(function(r){setButtonBusy(btn,false);showMsg('商品を追加しました。商品ID：'+r.productId,'success');byId('newProductName').value='';loadProductAdminMaster();loadInventory();}).withFailureHandler(function(e){setButtonBusy(btn,false);showMsg(errorText(e),'error');}).addProduct(payload);}\nfunction getExcludedStoresForProduct(id){var excluded=[];for(var i=0;i<availableStores.length;i++){var checkbox=byId('mex_'+id+'_'+i);if(checkbox&&checkbox.checked)excluded.push(availableStores[i]);}return excluded;}\nfunction productMasterPayload(id,active){return {pin:adminPin,productId:id,category:byId('mcat_'+id).value,name:byId('mname_'+id).value,unit:byId('munit_'+id).value,minStock:byId('mmin_'+id).value,suggestedQty:byId('msug_'+id).value,packQty:byId('mpack_'+id).value,orderUnitName:byId('mordername_'+id).value,supplier:byId('sup_'+id).value,orderUrl:byId('url_'+id).value,imageUrl:byId('img_'+id).value,productMemo:byId('memo_'+id).value,excludedStores:getExcludedStoresForProduct(id),active:active};}\nfunction saveProductMaster(id,active,btn){setButtonBusy(btn,true,'保存中…');google.script.run.withSuccessHandler(function(){setButtonBusy(btn,false);showMsg('商品情報と仕入先情報を保存しました。','success');loadProductAdminMaster();loadInventory();loadHQOrders();}).withFailureHandler(function(e){setButtonBusy(btn,false);showMsg(errorText(e),'error');}).updateProductMaster(productMasterPayload(id,active));}\nfunction toggleProductActive(id,newActive){var msg=newActive?'この商品を再表示しますか？':'この商品をアプリ上で非表示にしますか？ 過去の履歴は残ります。';if(!window.confirm(msg))return;google.script.run.withSuccessHandler(function(){showMsg(newActive?'商品を再表示しました。':'商品を非表示にしました。','success');loadProductAdminMaster();loadInventory();loadHQOrders();}).withFailureHandler(function(e){showMsg(errorText(e),'error');}).updateProductMaster(productMasterPayload(id,newActive));}\nfunction uploadProductImageFile(id,btn){var input=byId('file_'+id);if(!input||!input.files||!input.files[0]){showMsg('アップロードする画像を選択してください。','error');return;}var file=input.files[0];if(String(file.type).indexOf('image/')!==0){showMsg('画像ファイルを選択してください。','error');return;}if(file.size>5*1024*1024){showMsg('画像サイズは5MB以下にしてください。','error');return;}setButtonBusy(btn,true,'アップロード中…');var reader=new FileReader();reader.onload=function(){showMsg('画像をアップロードしています…','info');google.script.run.withSuccessHandler(function(r){setButtonBusy(btn,false);var imageUrl=String(r&&r.imageUrl||'');var imageInput=byId('img_'+id);if(imageInput)imageInput.value=imageUrl;for(var i=0;i<products.length;i++){if(String(products[i].id)===String(id)){products[i].imageUrl=imageUrl;break;}}for(var j=0;j<adminProducts.length;j++){if(String(adminProducts[j].id)===String(id)){adminProducts[j].imageUrl=imageUrl;break;}}var viewButton=byId('viewimg_'+id);if(viewButton)viewButton.style.display=imageUrl?'inline-block':'none';if(input)input.value='';showMsg('商品画像をアップロードしました。','success');loadHQOrders();}).withFailureHandler(function(e){setButtonBusy(btn,false);showMsg(errorText(e),'error');}).uploadProductImage({pin:adminPin,productId:id,dataUrl:reader.result,fileName:file.name});};reader.onerror=function(){setButtonBusy(btn,false);showMsg('画像の読み込みに失敗しました。','error');};reader.readAsDataURL(file);}\n\nfunction getEmbeddedAppVersion(){var node=document.querySelector('[data-app-version]');return String(node&&node.getAttribute('data-app-version')||'').replace(/^\\s+|\\s+$/g,'');}\nfunction showSystemInfoLoadFailure(e){var version=getEmbeddedAppVersion(),current=byId('sysCurrentVersion'),box=byId('updateResult');if(current)current.textContent=version?'v'+version+'（画面表示）':'バージョン情報を取得できません';if(box){box.style.display='block';box.innerHTML='<strong>更新情報の取得に失敗しました。</strong><br>通信またはGoogle Apps Scriptの実行状態を確認して、「情報を更新」をもう一度押してください。';}showMsg(errorText(e),'error');}\nfunction loadSystemUpdateInfo(btn){\n  var embeddedVersion=getEmbeddedAppVersion(),current=byId('sysCurrentVersion');\n  if(embeddedVersion&&current)current.textContent='v'+embeddedVersion;\n  if(btn)setButtonBusy(btn,true,'読込中…');\n  google.script.run\n    .withSuccessHandler(function(info){\n      if(btn)setButtonBusy(btn,false);\n      byId('sysCurrentVersion').textContent='v'+String(info.currentVersion||embeddedVersion||'不明');\n      var box=byId('updateResult');\n      box.style.display='block';\n      box.innerHTML='<strong>現在バージョン：</strong> v'+esc(info.currentVersion||'')+\n        '<br><strong>最終確認：</strong> '+esc(info.lastChecked||'未確認')+\n        '<br><strong>最終確認バージョン：</strong> v'+esc(info.lastVersion||info.currentVersion||'');\n      byId('applyUpdateBtn').style.display='none';\n    })\n    .withFailureHandler(function(e){\n      if(btn)setButtonBusy(btn,false);\n      showSystemInfoLoadFailure(e);\n    })\n    .getSystemUpdateInfo();\n}\n\nfunction checkSystemUpdate(btn){\n  setButtonBusy(btn,true,'確認中…');\n  byId('applyUpdateBtn').style.display='none';\n  google.script.run\n    .withSuccessHandler(function(r){\n      setButtonBusy(btn,false);\n      var box=byId('updateResult');\n      box.style.display='block';\n      var html='<strong>現在：</strong> v'+esc(r.currentVersion||'')+\n        '<br><strong>最新版：</strong> v'+esc(r.latestVersion||'');\n      if(r.publishedAt)html+='<br><strong>公開日時：</strong> '+esc(r.publishedAt);\n      if(r.notes)html+='<br><br><strong>更新内容</strong><br>'+esc(r.notes).replace(/\\n/g,'<br>');\n      html+='<br><br><strong>'+ (r.hasUpdate?'更新できます。':'現在のバージョンが最新です。') +'</strong>';\n      box.innerHTML=html;\n      byId('applyUpdateBtn').style.display=r.hasUpdate?'inline-block':'none';\n      showMsg(r.hasUpdate?'新しいバージョンが見つかりました。':'現在のバージョンが最新です。','success');\n    })\n    .withFailureHandler(function(e){\n      setButtonBusy(btn,false);\n      showMsg(errorText(e),'error');\n    })\n    .checkForAppUpdate();\n}\n\nfunction applySystemUpdate(btn){\n  setButtonBusy(btn,true,'更新中…');\n  google.script.run\n    .withSuccessHandler(function(r){\n      byId('applyUpdateBtn').style.display='none';\n      var box=byId('updateResult');\n      box.style.display='block';\n      var seconds=3;\n      function renderReloadCountdown(){\n        box.innerHTML='<strong>更新完了</strong><br>v'+esc(r.version||'')+' へ更新しました。'+\n          (r.backupFileName?'<br>バックアップ：'+esc(r.backupFileName):'')+\n          '<br><br><span class=\"update-important\">'+seconds+'秒後に最新版の反映確認を開始します。</span>';\n      }\n      renderReloadCountdown();\n      showMsg(r.message||'最新版への更新が完了しました。','success');\n      var reloadTimer=setInterval(function(){\n        seconds--;\n        if(seconds<=0){\n          clearInterval(reloadTimer);\n          waitForUpdatedDeployment(r.version,box,0);\n          return;\n        }\n        renderReloadCountdown();\n      },1000);\n    })\n    .withFailureHandler(function(e){\n      setButtonBusy(btn,false);\n      showMsg(errorText(e),'error');\n    })\n    .applyAppUpdate({});\n}\n\nfunction waitForUpdatedDeployment(targetVersion,box,attempt){\n  box.innerHTML='<span class=\"update-important\">最新版の反映を確認しています…</span><br><span class=\"muted\">確認中 '+(attempt+1)+' / 8回</span>';\n  google.script.run\n    .withSuccessHandler(function(info){\n      if(info&&info.ready){\n        box.innerHTML='<span class=\"update-important\">最新版の反映を確認しました。再読み込みしています…</span>';\n        var serviceUrl=String(info.serviceUrl||'').replace(/^\\s+|\\s+$/g,'');\n        var nextUrl=serviceUrl+(serviceUrl.indexOf('?')>=0?'&':'?')+'updated='+encodeURIComponent(String(targetVersion||''))+'&t='+Date.now();\n        setTimeout(function(){window.location.replace(nextUrl);},1500);\n        return;\n      }\n      scheduleDeploymentCheck(targetVersion,box,attempt);\n    })\n    .withFailureHandler(function(){scheduleDeploymentCheck(targetVersion,box,attempt);})\n    .getWebAppReadiness(targetVersion);\n}\n\nfunction scheduleDeploymentCheck(targetVersion,box,attempt){\n  if(attempt>=7){\n    box.innerHTML='<span class=\"update-important\">最新版の公開反映を確認できませんでした。</span><br><span class=\"muted\">画面を白くしないため、自動再読み込みは行っていません。10秒ほど待ってから下のボタンで再読み込みしてください。</span><br><button type=\"button\" class=\"secondary\" style=\"margin-top:10px\" onclick=\"reloadUpdatedApp(&quot;'+esc(String(targetVersion||''))+'&quot;)\">最新版を再読み込み</button>';\n    return;\n  }\n  setTimeout(function(){waitForUpdatedDeployment(targetVersion,box,attempt+1);},2000);\n}\n\nfunction reloadUpdatedApp(targetVersion){\n  var base=window.location.href.split('?')[0];\n  window.location.replace(base+'?updated='+encodeURIComponent(String(targetVersion||''))+'&t='+Date.now());\n}\n\nfunction normalizeImageUrl(url){var u=String(url||'').replace(/^\\s+|\\s+$/g,''),m=u.match(/drive\\.google\\.com\\/file\\/d\\/([^/]+)/);if(!m)m=u.match(/[?&]id=([^&]+)/);if(m&&m[1])return 'https://drive.google.com/thumbnail?id='+encodeURIComponent(m[1])+'&sz=w1200';return u;}\nfunction openProductImage(id){var p=findProduct(id);if(!p){for(var i=0;i<hqOrders.length;i++){if(hqOrders[i].productId===id){p=hqOrders[i];break;}}}if(!p||!p.imageUrl){showMsg('商品画像が登録されていません。','error');return;}byId('modalProductName').textContent=p.name||p.productName||'';byId('modalProductMemo').textContent=p.productMemo||'';var u=normalizeImageUrl(p.imageUrl);var area=byId('modalImageArea');var img=document.createElement('img');img.className='product-image';img.alt=p.name||p.productName||'商品画像';img.src=u;img.onerror=function(){area.innerHTML='<div class=\"image-error\">画像を表示できません。画像URLまたは共有設定を確認してください。</div>';};area.innerHTML='';area.appendChild(img);byId('imageModal').className='modal open';}\nfunction decorateProductMasterCards(){var host=byId('productAdminMaster');if(!host)return;var rows=host.getElementsByClassName('master-row');for(var i=0;i<rows.length;i++){var row=rows[i];if(row.getAttribute('data-decorated')==='true')continue;var p=adminProducts[i]||{};var heading=row.querySelector('.pname');if(!heading)continue;var complete=!!(String(p.orderUrl||'').trim()&&String(p.imageUrl||'').trim());var original=heading.innerHTML;heading.className+=' master-heading';heading.innerHTML='<span class=\"master-heading-main\">'+original+'<span class=\"master-status '+(p.orderUrl?'':'missing')+'\">発注ページ'+(p.orderUrl?'登録済み':'未登録')+'</span><span class=\"master-status '+(p.imageUrl?'':'missing')+'\">画像'+(p.imageUrl?'登録済み':'未登録')+'</span></span><button type=\"button\" class=\"secondary master-toggle\" onclick=\"toggleMasterDetails(this)\">'+(complete?'詳細を開く':'詳細を閉じる')+'</button>';row.setAttribute('data-decorated','true');if(complete)row.className+=' master-collapsed';}}\nfunction toggleMasterDetails(button){var row=button;while(row&&String(row.className||'').indexOf('master-row')<0)row=row.parentNode;if(!row)return;var collapsed=String(row.className||'').indexOf('master-collapsed')>=0;if(collapsed){row.className=row.className.replace(/\\s*master-collapsed/g,'');button.textContent='詳細を閉じる';}else{row.className+=' master-collapsed';button.textContent='詳細を開く';}}\nfunction setAllMasterDetails(open){var rows=byId('productAdminMaster').getElementsByClassName('master-row');for(var i=0;i<rows.length;i++){var row=rows[i],button=row.querySelector('.master-toggle');row.className=row.className.replace(/\\s*master-collapsed/g,'');if(!open)row.className+=' master-collapsed';if(button)button.textContent=open?'詳細を閉じる':'詳細を開く';}}\nfunction decorateHQArrangementFields(){var ranges=document.getElementsByClassName('delivery-range');for(var i=0;i<ranges.length;i++){var first=ranges[i].getElementsByTagName('input')[0];if(!first)continue;var row=String(first.id||'').replace('deliveryFrom_','');if(!row||byId('arrangement_'+row))continue;var box=document.createElement('div');box.id='arrangement_'+row;box.className='arrangement-box';box.innerHTML='<div class=\"arrangement-title\">手配内容（必要な場合だけ選択）</div><div class=\"arrangement-options\"><label class=\"arrangement-option\"><input type=\"radio\" name=\"arrangementType_'+Number(row)+'\" value=\"\">通常どおり</label><label class=\"arrangement-option\"><input type=\"radio\" name=\"arrangementType_'+Number(row)+'\" value=\"売り切れのため代替商品へ変更\">売り切れのため変更</label><label class=\"arrangement-option\"><input type=\"radio\" name=\"arrangementType_'+Number(row)+'\" value=\"送料を考慮して数量を変更\">送料を考慮して数量変更</label></div><input id=\"arrangementDetail_'+Number(row)+'\" class=\"arrangement-detail\" placeholder=\"補足があれば入力（任意）\">';var normal=box.querySelector('input[value=\"\"]');if(normal)normal.checked=true;ranges[i].parentNode.insertBefore(box,ranges[i].nextSibling);}}\nfunction decorateSavedArrangements(){var cards=document.getElementsByClassName('hq-order');for(var i=0;i<cards.length;i++){var card=cards[i];if(card.querySelector('.arrangement-saved'))continue;var text=String(card.textContent||''),item=null;for(var j=0;j<hqOrders.length;j++){if(text.indexOf(String(hqOrders[j].orderId||''))>=0){item=hqOrders[j];break;}}if(!item)continue;var type=String(item.arrangementType||''),detail=String(item.arrangementDetail||'');if(!type&&!detail)continue;var saved=document.createElement('div');saved.className='arrangement-saved';saved.textContent='手配内容：'+(type||'補足あり')+(detail?' ／ '+detail:'');var actions=card.querySelector('.hq-actions');if(actions)card.insertBefore(saved,actions);else card.appendChild(saved);}}\nfunction setLineStatus(row,status,btn){var mode=byId('deliveryMode_'+row);var deliveryUndecided=!!(mode&&mode.value==='undecided');var deliveryFrom=byId('deliveryFrom_'+row)?byId('deliveryFrom_'+row).value:'';var deliveryTo=byId('deliveryTo_'+row)?byId('deliveryTo_'+row).value:'';if(status==='手配済み'&&!deliveryUndecided&&!deliveryFrom){showMsg('納品予定の開始日を入力するか「未確定」を選択してください。','error');return;}if(status==='手配済み'&&!deliveryUndecided&&!deliveryTo)deliveryTo=deliveryFrom;if(!deliveryUndecided&&deliveryTo&&deliveryFrom&&deliveryTo<deliveryFrom){showMsg('終了日は開始日以降にしてください。','error');return;}var choice=document.querySelector('input[name=\"arrangementType_'+Number(row)+'\"]:checked');var arrangementType=choice?choice.value:'';var detail=byId('arrangementDetail_'+row)?byId('arrangementDetail_'+row).value:'';setButtonBusy(btn,true,'更新中…');google.script.run.withSuccessHandler(function(){setButtonBusy(btn,false);showMsg('手配済みに更新しました。','success');loadHQOrders();}).withFailureHandler(function(e){setButtonBusy(btn,false);showMsg(errorText(e),'error');}).updateOrderLineStatus({pin:adminPin,rowNumber:row,status:status,deliveryFrom:deliveryFrom,deliveryTo:deliveryTo,deliveryUndecided:deliveryUndecided,arrangementType:arrangementType,arrangementDetail:detail});}\nfunction renderHistory(rows){rows=rows||[];var html='';for(var i=0;i<rows.length;i++){var r=rows[i];var delivery=(r.deliveryFrom||r.deliveryTo)?'<div class=\"history-delivery\">納品予定：'+esc(r.deliveryFrom||r.deliveryTo)+(r.deliveryTo&&r.deliveryTo!==r.deliveryFrom?' ～ '+esc(r.deliveryTo):'')+'</div>':'';var arrangement=(r.arrangementType||r.arrangementDetail)?'<div class=\"arrangement-saved\">手配内容：'+esc(r.arrangementType||'補足あり')+(r.arrangementDetail?' ／ '+esc(r.arrangementDetail):'')+'</div>':'';var cancel=r.status==='発注依頼'?'<div class=\"history-actions\"><button class=\"danger\" onclick=\"cancelHistoryOrder('+Number(r.rowNumber)+',&quot;'+escAttr(r.orderId)+'&quot;,&quot;'+escAttr(r.store)+'&quot;,this)\">発注を取り下げる</button></div>':'';html+='<div class=\"history-item '+(r.status==='キャンセル'?'status-cancelled':'')+'\"><div class=\"pname\">'+esc(r.productName)+' × '+esc(r.qty)+esc(r.unit)+'</div><div class=\"history-date\">発注日：'+esc(r.date||'日時不明')+'</div><div class=\"history-details\">発注番号：'+esc(r.orderId)+'<br>店舗：'+esc(r.store)+' ／ 担当者：'+esc(r.user||'未入力')+' ／ 状態：'+esc(r.status)+'</div>'+delivery+arrangement+cancel+'</div>';}byId('history').innerHTML=html||'<div class=\"muted\">履歴はありません。</div>';}\nfunction enableProductMasterDragAndDrop(){var host=byId('productAdminMaster');if(!host)return;var toolbar=document.querySelector('.master-toolbar');if(toolbar&&!byId('saveProductOrderBtn')){var save=document.createElement('button');save.id='saveProductOrderBtn';save.type='button';save.className='secondary master-order-save';save.disabled=true;save.textContent='並び順を保存';save.onclick=function(){saveProductDisplayOrder(this);};toolbar.insertBefore(save,toolbar.firstChild);}var rows=host.getElementsByClassName('master-row');for(var i=0;i<rows.length;i++){var row=rows[i],product=adminProducts[i]||{};if(!row.getAttribute('data-product-id'))row.setAttribute('data-product-id',String(product.id||''));if(row.getAttribute('data-drag-ready')==='true')continue;var heading=row.querySelector('.master-heading');if(!heading)continue;var handle=document.createElement('button');handle.type='button';handle.className='master-drag-handle';handle.setAttribute('draggable','true');handle.setAttribute('aria-label','商品をドラッグして並び替え');handle.title='ドラッグして並び替え';handle.textContent='⋮⋮';heading.insertBefore(handle,heading.firstChild);handle.addEventListener('dragstart',function(e){var current=this;while(current&&String(current.className||'').indexOf('master-row')<0)current=current.parentNode;host.setAttribute('data-dragging-id',current&&current.getAttribute('data-product-id')||'');if(e.dataTransfer){e.dataTransfer.effectAllowed='move';e.dataTransfer.setData('text/plain',host.getAttribute('data-dragging-id'));}});handle.addEventListener('dragend',function(){host.removeAttribute('data-dragging-id');var all=host.getElementsByClassName('master-row');for(var k=0;k<all.length;k++)all[k].className=all[k].className.replace(/\\s*drag-over/g,'');});row.addEventListener('dragover',function(e){if(!host.getAttribute('data-dragging-id'))return;e.preventDefault();e.dataTransfer.dropEffect='move';this.className+=' drag-over';});row.addEventListener('dragleave',function(){this.className=this.className.replace(/\\s*drag-over/g,'');});row.addEventListener('drop',function(e){e.preventDefault();var sourceId=host.getAttribute('data-dragging-id'),source=null;var all=host.getElementsByClassName('master-row');for(var n=0;n<all.length;n++)if(all[n].getAttribute('data-product-id')===sourceId)source=all[n];this.className=this.className.replace(/\\s*drag-over/g,'');if(!source||source===this)return;var rect=this.getBoundingClientRect();host.insertBefore(source,e.clientY>rect.top+rect.height/2?this.nextSibling:this);markProductOrderDirty();});row.setAttribute('data-drag-ready','true');}}\nfunction markProductOrderDirty(){var save=byId('saveProductOrderBtn');if(save)save.disabled=false;}\nfunction saveProductDisplayOrder(button){var host=byId('productAdminMaster'),rows=host?host.getElementsByClassName('master-row'):[],ids=[];for(var i=0;i<rows.length;i++){var id=String(rows[i].getAttribute('data-product-id')||'').trim();if(id)ids.push(id);}if(!ids.length){showMsg('商品がありません。','error');return;}setButtonBusy(button,true,'保存中…');google.script.run.withSuccessHandler(function(){setButtonBusy(button,false);showMsg('商品表示順を保存しました。','success');loadProductAdminMaster();loadInventory();}).withFailureHandler(function(e){setButtonBusy(button,false);showMsg(errorText(e),'error');}).updateProductDisplayOrder({pin:adminPin,productIds:ids});}\nfunction closeImageModal(){byId('imageModal').className='modal';byId('modalImageArea').innerHTML='';}\nfunction showTab(name,button){var names=['inventory','cart','history','system','hq'];for(var i=0;i<names.length;i++){byId(names[i]+'Tab').style.display=names[i]===name?'block':'none';}document.body.classList.toggle('hq-mode',name==='hq');var tabs=document.getElementsByClassName('tab');for(i=0;i<tabs.length;i++){tabs[i].className='tab';}if(button)button.className='tab active';}\nvar toastTimer=null;\nfunction showMsg(text,cls){var host=byId('msg');if(!host)return;if(toastTimer)clearTimeout(toastTimer);host.innerHTML='';var t=document.createElement('div');t.className='toast '+(cls||'info');t.textContent=text||'';host.appendChild(t);setTimeout(function(){t.className+=' show';},10);toastTimer=setTimeout(function(){t.className=t.className.replace(' show','');setTimeout(function(){host.innerHTML='';},220);},3200);}\nfunction setButtonBusy(btn,busy,label){if(!btn)return;if(busy){if(!btn.getAttribute('data-label'))btn.setAttribute('data-label',btn.textContent);btn.disabled=true;btn.className+=(btn.className.indexOf('btn-busy')<0?' btn-busy':'');btn.innerHTML='<span class=\"btn-spinner\"></span>'+esc(label||'処理中…');}else{btn.disabled=false;btn.className=btn.className.replace(/\\s*btn-busy/g,'');btn.textContent=btn.getAttribute('data-label')||'完了';btn.removeAttribute('data-label');}}\nfunction esc(s){return String(s===null||typeof s==='undefined'?'':s).replace(/[&<>\"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[c];});}\nfunction escAttr(s){return esc(s).replace(/`/g,'&#96;');}\nrenderCart();\nif(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init);}else{init();}\n</script>\n</body>\n</html>\n"
    },
    {
      "name": "appsscript",
      "type": "JSON",
      "source": "{\n  \"timeZone\": \"Asia/Tokyo\",\n  \"dependencies\": {},\n  \"exceptionLogging\": \"STACKDRIVER\",\n  \"runtimeVersion\": \"V8\",\n  \"webapp\": {\n    \"access\": \"ANYONE_ANONYMOUS\",\n    \"executeAs\": \"USER_DEPLOYING\"\n  },\n  \"oauthScopes\": [\n    \"https://www.googleapis.com/auth/spreadsheets\",\n    \"https://www.googleapis.com/auth/drive\",\n    \"https://www.googleapis.com/auth/script.send_mail\",\n    \"https://www.googleapis.com/auth/script.external_request\",\n    \"https://www.googleapis.com/auth/script.projects\",\n    \"https://www.googleapis.com/auth/script.deployments\"\n  ]\n}"
    }
  ]
}
